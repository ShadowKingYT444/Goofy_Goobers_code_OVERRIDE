COM6 console is silent even though the robot program is running.
Brain program uploads but user code does not run.
Screen barcode coordinates are shifted by the V5 title bar.
Distance sensor is installed but returns low-confidence near-zero readings.
