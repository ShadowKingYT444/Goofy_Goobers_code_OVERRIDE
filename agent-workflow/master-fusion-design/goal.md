Replace the obsolete contents of `NEXT_CHANGES.md` with an implementation-ready design for the next localization system. The design must distinguish current behavior from proposed behavior, compare every fresh external measurement, weight corrections by estimator uncertainty and measurement quality, reject outliers, and cover LiDAR, AI Vision, drivetrain encoders, horizontal odometry, IMU, telemetry, recovery, and physical evaluation.

This is a design pass. Do not claim that the proposed master fusion estimator is already implemented.
