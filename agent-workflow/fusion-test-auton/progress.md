2026-07-08: Started fusion test auton work. Baseline inspection found the current code already has IMU heading, LiDAR theta bias correction, wall winner-margin gating, side odometry, and motor encoder fusion. X+Down is currently occupied by PID autotune, so it must be remapped for the requested test.
2026-07-08: Added `fusion_test_auton()` with a fused-pose 12 in forward, 45 deg clockwise turn, 12 in forward route. Changed competition autonomous to run it and changed X+Down to trigger it from opcontrol. B+Down still runs the older bypass route.
2026-07-08: Added and ran `fusion_test_source_check.py`; it passed after adjusting the checker to look for the actual `log_pose(...)` call sites. Tried `pros.exe make` with APPDATA redirected into `.pros-appdata`; PROS still failed to launch the VS Code global `make.exe` with WinError 5 Access is denied.
2026-07-08: User reported the first uploaded X+Down test drove backward/turned immediately. Replaced the fused waypoint follower control path with a real physical-sign test: direct PROS motor commands use left negative/right positive for forward, IMU rotation for the 45 degree clockwise turn, encoder distance for each 12 inch leg, and `update_pose()` logs fusion data throughout. Reran source check successfully. PROS CLI version and `lsusb --target v5` worked and detected COM8/COM9, but `python -m pros.cli.main make` still failed on the global make.exe WinError 5 access blocker.
2026-07-08: User reported the corrected direct-power loop tracked degrees but moved too slowly, left large gaps, did not complete full 12 in / 45 deg moves, and should use normal PROS movements. Replaced forward legs with PROS `motor.move_relative()` at 220 rpm and replaced the turn with EZ `pid_turn_relative_set(..., 85, ...)`. Fusion still updates during waits. LiDAR/IMU correction period is now 1 second, and accepted wall-theta corrections call `chassis.drive_imu_reset(...)` so the drive IMU used by turns is corrected. Source check passed; PROS make still failed with the external make.exe WinError 5 blocker.
2026-07-08: User clarified that the test must be coordinate-first so odometry is useful, movements should be chained, and LiDAR wall should only correct IMU heading for now. Reworked `fusion_test_auton()` to define absolute field waypoints (`wp1`, `turn_heading`, `wp2`) before commanding movement, derive each move from the fused pose, shorten chain samples to 40 ms, and remove LiDAR x/y pose correction. Source check passed, PROS build passed, V5 was detected on COM8/COM9, and the program uploaded to V5 slot 1 with `--after none`.
2026-07-08: User reported a long pause after the first drive, the 45 degree turn drove backward, and the second drive went far above 12 inches. Replaced `move_relative()` waits with encoder-bounded direct PROS drive, removed EZ turn from the diagnostic and used direct IMU error with physical turn signs, capped each coordinate drive command to its planned 12 inch segment, and kept LiDAR wall fusion as 1 Hz IMU correction only. Source check passed, PROS build passed, and the corrected binary uploaded to V5 slot 1 with `--after none`.
2026-07-08: User reported the pose grid showed turn oscillation/squiggles and the robot bounced between roughly 10 and 30 degrees instead of reaching 45. Added an `allow_lidar_correction` gate to `update_pose()`, disabled LiDAR IMU resets during active drive/turn loops, and replaced the old hard-minimum turn power law with damped kP/kD `command_cw` plus slew limiting. Final constants for this upload: Kp 0.95, Kd 0.08, min turn power 12, max turn power 60, turn timeout 1600 ms. Source check passed, PROS build passed, and the corrected binary uploaded to V5 slot 1 with `--after none`.
2026-07-08: User asked for movements that actually use the full fused system, not only logging fused pose while controlling from encoder distance/raw IMU. Added `LidarFusionMode`, soft LiDAR bias mode, `fused_drive_to_point()`, and `fused_turn_to_heading()`. X+Down now drives wp1, turns 45 degrees, and drives wp2 from fused x/y/heading every loop, using drivetrain encoders, horizontal odom, IMU, and soft LiDAR wall theta bias when available. Source check passed, PROS build was current, and the full-fused controller binary uploaded to V5 slot 1 with `--after none`.
