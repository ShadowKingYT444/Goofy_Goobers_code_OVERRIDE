2026-07-07: Started odometry UI change. Verified drivetrain ports from src/main.cpp and researched VEX field as 12 ft x 12 ft.
2026-07-07: Added field map UI, relative differential-drive odometry, and explicit left-stick sign fix in opcontrol. Python syntax passed. PROS make failed with WinError 5 access denied from make.exe, so C++ upload verification is blocked.
2026-07-07: Rebuilt and uploaded successfully after permissions changed. Fixed motor parser to tolerate m17=inf and fixed browser lookup from latest.motors to latest.latest.motors. Verified Chrome shows motor telemetry and stable big-readout styles.
2026-07-07: User reported clockwise turns drew counterclockwise. Flipped browser odometry heading delta from right-left to left-right. Verified page reloads, motor telemetry remains visible, and big readouts keep stable 72px text styling.
2026-07-07: Live motor signs indicated the left encoder reports opposite sign from the right. Added leftEncoderSign=-1 and returned heading math to standard right-left using normalized wheel distances. Verified dashboard reloads with motor telemetry and stable 72px readouts.
2026-07-07: Added odometry_sign_check.py to lock the sign convention: forward from bottom-left increases Y, clockwise decreases raw heading from 90 deg, and counterclockwise increases it. The check passes.
2026-07-07: Added Reset Origin button plus normalized Left Wheel and Right Wheel travel readouts. Verified in Chrome that the button renders, motor telemetry is visible, and the wheel readouts render.
2026-07-07: Completion audit reran py_compile, odometry_sign_check, PROS make, live /data check, and Chrome DOM verification. All passed, but physical motion verification remains unproven because motor encoder values were stationary during the sample.
2026-07-07: Sampled live encoders for 8 seconds and saw a small right-side-only delta. Added Last Delta and Turn Delta readouts so movement/sign behavior is visible in the dashboard while driving.
2026-07-07: Resampled live endpoint for 6 seconds; motor positions were unchanged, producing normalized deltas of 0. Browser pose diagnostics also showed Last Delta L 0.00 / R 0.00 and Turn Delta 0.00 deg, so a stationary dot is expected until encoders change.
2026-07-07: Added port 5 horizontal odometer plan details after user clarified the odometer is centered, 12 cm behind center, 2 in wheel diameter, and LiDAR faces left. Implementation will use h5 centidegree telemetry and LiDAR as helper-only wall correction under 50 in.
2026-07-07: Implemented h5 telemetry, browser-side side odometry, and left-facing LiDAR wall assist. Python syntax, odometry signs, telemetry parser checks, and PROS make passed. Upload to COM15 succeeded and live /data on COM14 reports odometer port 5 position_centideg.
2026-07-07: Added red field-map LiDAR visualization: center measurement ray, RMSE-spaced probability bands, and theta label. Verified Python syntax, telemetry parser check, embedded dashboard JS syntax, live /data on COM14, and served HTML markers.
2026-07-07: Replaced the single LiDAR field overlay with four ambiguous wall hypotheses. Each hypothesis draws red RMSE bands, a central red line, theta label rotated by 90 deg increments, and arrows every 12 in along the line direction. Verified Python syntax, embedded JS syntax, parser check, and live COM14 dashboard serving the new code.
2026-07-07: Corrected the LiDAR hypothesis overlay so theta only changes arrow direction and labels. The red hypothesis lines now remain parallel to their walls. Verified Python syntax, embedded JS syntax, restarted dashboard, and confirmed live COM14 data.
2026-07-07: Corrected LiDAR distance geometry. Field wall hypotheses and LiDAR assist now use perpendicular wall distance computed as center beam distance / sqrt(1 + slope^2), equivalent to center distance x cos(theta). The raw 50 in sensor gate remains based on LiDAR-reported distances. Verified Python syntax, embedded JS syntax, parser check, restarted dashboard, and confirmed served Wall Distance UI.
2026-07-07: Added dashboard tabs and field-only route. `/playing-field`, `/playing_field`, `/playingfield`, and `/playing%20field` serve the same live field canvas with non-field sections hidden. Verified Python syntax, rendered HTML placeholders, embedded JS syntax, parser check, route HTML markers, 144 in field size, and live COM14 /data at 50 Hz.
2026-07-07: Added last-good LiDAR hold behavior. When current LiDAR gate/RMSE is bad or there are too few valid points, the field overlay and big LiDAR readouts keep the previous good wall estimate while pose correction remains disabled. Verified Python syntax, embedded JS syntax, parser check, restarted dashboard, and confirmed served field-only HTML contains the hold state/text.
2026-07-07: Added drivetrain-encoder vertical displacement overlay. The field canvas now draws a cyan horizontal drive Y line from normalized drivetrain encoder forward displacement plus a vertical arrow from the bottom edge. Verified Python syntax, embedded JS syntax, parser check, served field-only HTML markers, live COM14 at 50 Hz, and saved a Chrome screenshot artifact.
2026-07-07: Updated LiDAR correction to projection instead of smoothing. On a good LiDAR gate, the code scores wall hypotheses by current odometry distance error plus odometry-heading theta error, then projects the pose onto the selected wall-distance line. Bad LiDAR still leaves pose odometry-only. Verified Python syntax, embedded JS syntax, parser check, served field-only HTML markers, and live COM14 at 50 Hz.
2026-07-08: User asked for a simple grid to see robot position/heading according to the full fusion system while debugging a bad 45 degree turn. Extended `tools/lidar_bar_server.py` to parse onboard `FUSE_TEST` pose lines, expose `onboard_pose` in `/data`, add `/pose-grid`, and add a canvas HUD showing source/x/y/heading. The grid now prefers fresh onboard fused pose and falls back to browser D4 odometry when `FUSE_TEST` is stale or absent. Disabled browser-side LiDAR x/y projection so LiDAR remains heading/context only. Verified Python syntax, telemetry parser checks, odometry sign checks, embedded JS syntax, running server routes, and live `/data` on COM8 at 50 Hz.
