Commands:

```powershell
.\.venv\Scripts\pros.exe make
.\.venv\Scripts\pros.exe upload --slot 1 --name MoreVex --after run . COM15
```

Manual:

- Reset the web field pose before running.
- Start from the screenshot pose, facing upfield.
- Run autonomous and watch that the robot takes the right-side bypass around the goal.
- Confirm it ends near the centerline about 36 in forward.
