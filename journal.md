# Localization Work Journal

Append-only record of meaningful actions. Timestamps use the laptop's local
timezone (`America/Los_Angeles`).

- `2026-07-10T23:00:15.926-07:00` - Created this journal at the user's request. Earlier tonight, saved the supplied Discord field image as `agent-workflow/multisensor-localization-completion/field_setup_reference_2026-07-10.webp`; ran two captured four-segment turn correlations; found and fixed the incorrect right-drive encoder sign; pooled eight segments to configure a 12.0086-inch effective drive track width, 2.5665-inch effective rear-wheel lever, and 0.926770 LiDAR theta scale; restored all startup-motion flags to false; passed 13 regression tests; built and uploaded the no-motion production firmware. Live stationary verification is in progress.
- `2026-07-10T23:00:42.599-07:00` - Live no-motion verification passed on COM8: drive motors 17/18/11/13 all read 0.0 degrees, rear odometer port 5 read 0 centidegrees, raw/EZ IMU read -0.01 degrees, and the corrected LiDAR fit was clean (`theta=-0.63 deg`, `RMSE=0.040 in`). Fused pose was `(-47.78, 0.50, 88.15 deg)`. AI Vision reported `not_installed` on configured port 1, so a no-motion 21-port inventory is next.
- `2026-07-10T23:02:35.193-07:00` - Stopped the temporary localization server to release COM8. The first raw-inventory capture attempt failed because its child process still held the port; terminated only the two `lidar_bar_server.py` processes and verified COM8 reopened. Captured a no-motion program interval to `device_inventory_current.log`; it confirmed repeated AI Vision `not_installed` results on port 1, but the upload did not restart the running program early enough to recapture one-time `DEVICE` inventory lines. No motors moved. Began planning the requested web UI correlation readout and final report.
- `2026-07-10T23:05:58.964-07:00` - Added firmware-authoritative `track`, `rear`, and `lidar_scale` telemetry; corrected the browser fallback's duplicated right-encoder sign, track width, rear lever, LiDAR scale, confidence, RMSE, and point-error gates; added UI readouts for effective track, rear lever, predicted 15-degree rear-wheel travel, and predicted left +1/right -1 turn. Fourteen tests passed and firmware rebuilt. Recorded the user's confirmed hardware: eight non-strafing 2.75-inch omni drive wheels (four per side, two geared motors per side) and one 2-inch rear-center lateral omni tracking wheel.
- `2026-07-10T23:10:38.696-07:00` - The first 0.5-inch forward attempt did not execute because upload alone did not restart the running slot; the continuous Brain clock and zero encoders proved no motion. Repeated with explicit `v5 stop`/`v5 run` and raw capture. Live peak result: m17=21.0 deg, m18=20.0 deg, m11=22.0 deg, m13=22.0 deg, rear lateral odometer=0 centidegrees, IMU delta=-0.24 deg, `ok=1`, fused displacement about 0.51 inch. This validates same-sign drive encoders, negligible same-side spread, and zero forward cross-axis leakage. Restored both calibration flags to false, rebuilt, uploaded, explicitly restarted slot 1, and captured before/after camera frames.
- `2026-07-10T23:21:59.355-07:00` - Added AI Vision device-type auto-discovery across Smart Ports 1-21 and dynamic camera-port telemetry/UI labels. Ran the user's requested bounded 50-degree counterclockwise view change: final IMU delta was -49.48 degrees (49.48 degrees CCW), with no translation command. Auto-discovery found AI Vision on port 20 and configured Circle 21H7 tag-only mode successfully; the camera reported `no_tag` throughout the 50-degree view, consistent with this angle exposing the red block rather than the tag face. Captured `vision_turn_50ccw_before.png`, `vision_turn_50ccw_after.png`, and `vision_turn_50ccw.log`. Restored the movement flag and diagnostic target/guard, set preferred AI port 20, passed 15 tests, built/uploaded/restarted the no-motion firmware. Updated the current test start anchor to `(-47.78, 1.01, 138.23 deg)` so the post-turn reboot retains the measured physical pose.
- `2026-07-10T23:23:58.145-07:00` - Verified the post-turn anchor live: fused heading 137.72 deg versus 138.23 deg IMU anchor; AI Vision auto-discovered on port 20 and remained healthy/configured with `no_tag` at the requested 50-degree view. A pure-rotation regression appeared in LiDAR position correction: X moved from the entered -47.78 in anchor to -49.53 in while Y remained 1.01 in. Tightened the single-wall axis innovation gate from 12.0 to 1.5 inches so a red block/offset mismatch after turning cannot claim large translation without independent evidence.
- `2026-07-10T23:28:23.210-07:00` - Captured the fixed Brio 101 webcam through its existing local RTSP stream to `agent-workflow/multisensor-localization-completion/webcam_current_2026-07-10.png`. The frame clearly shows the red tagged block at the far right edge and the robot at the completed 50-degree CCW pose; onboard AI Vision P20 still reports `no_tag`. Enabled one supervised 8-degree CCW startup adjustment to bring the robot camera closer to the visible tag direction; this diagnostic flag will be restored false immediately after the trial.
- `2026-07-10T23:29:33.528-07:00` - The supervised 8-degree CCW adjustment succeeded. Live drivetrain endpoints were left -39/-41 deg and right +42/+41 deg; the passive rear-center odometer measured 20.04 sensor degrees. AI Vision P20 acquired AprilTag ID 3 with one object, valid quadrilateral geometry, center `(52,168) px`, area `224 px^2`, and bearing `-26.96 deg`. The pose observer correctly did not apply it because the current uncalibrated landmark/camera geometry produced a 41.27-degree bearing residual. No backward motion was needed. Restored startup motion false and advanced the physical heading anchor from 138.23 to 146.23 degrees for the final no-motion image.
- `2026-07-10T23:31:10.801-07:00` - Finalized the successful Tag 3 viewing pose. Ran `tests/test_localization_ui.py` (15/15 passed), rebuilt firmware, uploaded slot 1, explicitly stopped/restarted the program, and verified the production image has all startup motion disabled. Live no-motion telemetry is anchored at `(-47.78, 1.01, 146.23 deg)`, all four drive encoders and the rear odometer are zero after reboot, and AI Vision P20 continues to see Tag 3 at `-26.96 deg`. Captured the post-turn Brio frame as `agent-workflow/multisensor-localization-completion/webcam_tag3_pose_2026-07-10.png`. The observation remains rejected from pose correction as `bearing_residual`, which is safe until the robot-camera extrinsic and tag-face map are calibrated.
- `2026-07-10T23:31:59.577-07:00` - Read the renewed persistent objective from `pasted-text-1.txt` and audited current completion evidence. The full requirement is tournament localization from an exact stationary start using onboard IMU, drivetrain/rear odometry, LiDAR when unobstructed, and four-sided AprilTags on nine static props; the Brio webcam is debugging only. Current evidence still leaves LiDAR-dropout recovery, runtime start-pose entry, camera extrinsic/tag-face calibration, controller-route accuracy, and full acceptance incomplete. Root `report.md` is still missing. Kept the goal active and updated the five-stage completion plan.
- `2026-07-10T23:38:02.453-07:00` - Researched current official PROS/VEX AI Vision APIs: neither exposes a built-in AprilTag distance field; PROS provides ID plus four corners, while VEXcode provides pixel size/center/orientation/ID/score. Added mean tag-edge and provisional pinhole range telemetry, fixed the mirrored mapped-bearing sign, and changed duplicate-ID ambiguity scoring to compare distinct physical Goals rather than adjacent faces on the same Goal. Sixteen host tests passed, firmware built/uploaded, and live Tag 3 now selects `upper_red_neutral` with 1.98-degree residual and 89.14-degree cross-Goal margin (`shadow_valid`). Collected 30 stationary baseline samples: bearing mean -26.937 deg, mean edge 14.948 px, provisional range 10.649 in, area 221 px2. Enabled one supervised 2.0-inch forward calibration at 6 RPM to measure how tag bearing and apparent size respond; the movement flag and target will be restored immediately afterward.
- `2026-07-10T23:39:46.369-07:00` - Completed the supervised 2.0-inch one-way tag-motion trial. Drive endpoints were left 85/82 deg and right 76/76 deg, rear lateral odometer stayed exactly zero, and IMU stayed at 0.0 deg. Across 30 post-move samples, Tag 3 remained valid and `shadow_valid`; bearing shifted from -26.937 to -19.990 deg, mean edge from 14.948 to 14.523 px, provisional range from 10.649 to 10.962 in, and area from 221.0 to 210.25 px2. Two-view triangulation over the known 2-inch baseline gives about 15.5 inches range and implies about 1.08 inches physical tag width at the current focal calibration. Captured `tag3_after_2in_forward.png`. Generalized the bounded forward-calibration helper to accept signed distance and queued an equal 2.0-inch return to test reversibility before adopting the size/range calibration.
- `2026-07-10T23:41:50.960-07:00` - The equal signed return trial passed its reversibility check: motor endpoints were left -80/-77 deg and right -82/-87 deg, rear odometer stayed zero, IMU stayed 0.0 deg, Tag 3 bearing returned to -27.60 deg (0.66 deg from the original mean), edge returned to 15.06 px (0.11 px from baseline), and area returned to 224 px2. Using actual first-leg encoder travel of about 1.91 inches gives about 14.9 inches parallax range and a 1.05-inch effective outer tag size. Restored the startup movement flag false, restored the standard 0.5-inch diagnostic target/watchdog, set the calibrated tag size to 1.05 inches, and added tag range/edge readouts to the UI. The robot is back at the pre-test physical position within the measured return residual.
- `2026-07-10T23:42:53.692-07:00` - Final no-motion image after the reversible Tag 3 range trial passed 16/16 host tests, compiled, uploaded, and explicitly restarted. All four drive encoders and rear odometer are zero after reboot, startup movement is disabled, fused heading is 146.23 deg, and AI Vision P20 reports Tag 3 at -27.60 deg, 15.06 px mean edge, calibrated 14.80 in range, 2.62 deg map residual, 87.85 deg duplicate-Goal margin, and `shadow_valid`. Restarted the host localization server so the new range and edge fields are parsed and shown in the live UI.
- `2026-07-10T23:47:05.851-07:00` - Added AprilTag range-consistency diagnostics and hard gates before any future pose correction. Live production verification shows the current compiled pose/map predicts Tag 3 at 83.44 inches while the reversible camera calibration measures 14.80 inches, a 68.63-inch mismatch; the observer now explicitly reports `range_residual` and cannot use that tag to teleport the pose. Sixteen tests passed, firmware built/uploaded, and the UI server was restarted with observed/predicted/range-residual fields. The supplied overhead field image confirms the robot is physically near the back wall and close to the red Goal, so the current `(-47.78,1.01)` start anchor is not a valid absolute position for this setup. Enabled one supervised 8-degree CCW pure-rotation sample to solve the side-mounted camera offset from tag parallax; a matching return will follow before normal firmware is restored.
- `2026-07-10T23:51:48.540-07:00` - Researched the VEX AI Vision optical specifications. Official VEX documentation states 320x240 resolution, 74-degree horizontal FOV, 63-degree vertical FOV, and 87-degree diagonal FOV; no official radial/fisheye distortion coefficients or camera matrix are published. The previous config incorrectly reused horizontal focal length for Y, so added the correct 195.82 px vertical focal length alongside 212.34 px horizontal. The 8-degree CCW sample kept Tag 3 valid but shifted bearing -27.60 to -14.02 deg and edge 15.06 to 12.82 px; the scalar edge range changed 14.80 to 17.39 inches. Offline four-corner PnP with the documented asymmetric focal lengths estimated 17.23 and 17.60 inches instead, only 0.37 inches apart, proving all-four-corner planar pose is materially more rotation-invariant. Queued the equal 8-degree CW return before further firmware work.
- `2026-07-10T23:53:10.264-07:00` - The equal 8-degree CW return restored Tag 3 telemetry exactly: bearing -27.60 deg, edge 15.06 px, area 224 px2, and scalar range 14.80 in. Drive endpoints reversed and rear odometer reversed from +38.85 to -38.14 sensor degrees; final IMU residual was -0.01 deg. Fitting the documented-intrinsics four-corner PnP pair yielded a plausible front-right camera offset of approximately 6.75 in forward and 10.44 in right, consistent with the fixed webcam image. Stored those extrinsics in shadow config, replaced the isotropic edge-only range with separate horizontal/vertical focal scaling plus off-axis correction, restored the standard -8-degree diagnostic target, and disabled all startup motion.
- `2026-07-10T23:55:31.954-07:00` - Final no-motion firmware after the camera rotation trial passed 16 tests, built/uploaded, and verified all drive/rear encoders zero. The anisotropic/off-axis corner range reports 16.46 inches at the restored pose. Using the visible upper Tag 3 -X face `(20.745,47.10)`, camera heading `56.23 deg`, bearing `-27.60 deg`, measured range `16.46 in`, and calibrated camera offset `(forward=6.75,right=10.44) in` yields robot center approximately `(6.10,44.15) in`. This agrees with the overhead webcam layout (robot near the back wall and close to the red Goal), unlike the obsolete `(-47.78,1.01)` anchor. Updated the stationary debug start anchor to `(6.10,44.15,146.23 deg)` for a no-correction agreement test.
- `2026-07-10T23:57:53.507-07:00` - Four-corner PnP at the restored Tag 3 view estimated 17.23-inch range and a tag-plane normal only 19.45 degrees from head-on. The horizontal normal/ray direction identifies the visible physical face as the upper Tag 3 Goal's -X face, not the adjacent +Y candidate produced by the provisional offset. The reversible rotation fit had a lateral sign ambiguity; enforcing the observed -X face resolves it as camera right-offset -10.44 inches (camera on robot-left in the estimator frame), with forward offset +6.75 inches. Recomputed the current robot-center anchor as approximately `(17.02,43.77,146.23 deg)` and queued a stationary bearing-plus-range agreement test.
- `2026-07-11T00:00:49.171-07:00` - Enabled bounded onboard AprilTag pose correction after the stationary bearing/range agreement passed. The implementation requires three consistent observations, fresh valid geometry, duplicate-Goal margin, <=8-inch position innovation, <=8-inch range residual, 500 ms cadence, <=0.75-inch position step, and <=0.5-degree heading-bias step. With LiDAR continuously rejected as `fit`, live Tag 3 correction converged the pose from `(17.02,43.77)` to approximately `(17.74,44.16)` while heading stayed about 146.22 degrees and range residual fell from 0.78 to about 0.03 inches. This is live stationary evidence of tag-based correction during LiDAR unavailability. Added 0.10-inch/0.10-degree correction deadbands and regression assertions for the safety gates.
- `2026-07-11T00:03:35.472-07:00` - Completed a 30.5-second stationary AprilTag correction audit while LiDAR was unavailable/rejected on every sample. The first PowerShell collection command failed at parse time and collected nothing; the corrected run captured 61 samples to `tag3_lidar_outage_stationary_30s.csv`. Pose spans were X 0.16 in, Y 0.09 in, heading/IMU 0.01 deg; range span was 0.49 in. Every sample remained inside the correction deadband (`ai_pos_step=0`, `ai_heading_step=0`) and all 61 reported LiDAR `fit` rejection, proving stable tag maintenance without LiDAR at rest. Updated acceptance to record stationary tag fallback separately; full moving LiDAR-dropout recovery remains open for the tether-managed longer route.
- `2026-07-11T00:09:10.995-07:00` - User confirmed tether management for a longer route. Captured `long_route_pre.json` and `long_route_pre.png`; the Brio frame shows the red Goal near the robot's forward/right side and a clear backward path toward the fixed camera. Replaced the shorter 12-45-12 acceptance routine with an abort-on-first-failure tether-managed route: 20 inches backward, 20 inches forward return, stationary +90/-90 degree turn, then a second 20-inch backward/forward return. Total commanded travel is 80 inches and rotation 180 degrees. Generalized the encoder-bounded leg helper for signed travel and enabled the route for one supervised boot only.
- `2026-07-11T00:13:10.493-07:00` - Analyzed the first tether-managed route from 1,100 decoded telemetry packets and 1,360 Brio frames. The first 20-inch reverse/return pair completed, but the 90-degree turn stalled 14.25 degrees short at power 13.5; the route correctly aborted before cycle two. Brio start/end frames show the robot returned near its starting center but rotated about 76 degrees. Estimator return error before the turn was 1.89 inches; the 80.80-degree pure turn then invented another 2.63 inches of translation, identifying the rear-wheel lever as about 4.43 inches rather than the noisy 2.5665-inch micro-turn fit. Normal UI telemetry also snapped to the compiled start after the route because the local route estimator was not handed off. During the user's battery swap, delivered `v5 stop` immediately to prevent the still-flashed one-time route from restarting. Locally disabled startup motion, set the rear lever to 4.43 inches, raised minimum fused-turn power from 12 to 18, added a one-second turn-stall abort, and handed the route pose/baselines to normal telemetry.
- `2026-07-11T00:17:18.795-07:00` - After the user completed the battery swap, built and uploaded a no-startup-motion image with the current physical anchor `(17.02,43.77,221.89 deg)`. Sixteen tests passed. Live verification showed all four drive encoders and rear odometer zero, IMU zeroed/healthy, AI Vision P20 installed/configured with `no_tag`, and all four LiDAR sensors honestly out of range at 9999 mm/confidence 0. Prepared a one-boot continuation route that first turns 75.66 degrees back to the known clear travel heading using the raised minimum power/stall watchdog, then executes two 20-inch reverse/forward cycles (80 inches added travel, 75.66 degrees added rotation).
- `2026-07-11T00:20:14.384-07:00` - The continuation route completed successfully: recovery turn plus two 20-inch reverse/forward cycles, adding 80 inches travel and 75.66 degrees rotation. Combined tether-managed session travel was approximately 120 inches (10 feet). Raw route endpoint return error was 1.54 inches and heading error 4.93 degrees, with no abort/stall. Each translation pair independently returned within about 0.6-0.7 inches. The 73.74-degree recovery turn produced only 0.97 inches fake translation with the 4.43-inch rear lever, refining the lever to about 5.18 inches. After the estimator state was correctly handed to normal telemetry, Tag 3 reacquired and bounded fusion converged to `(22.91,41.15,144.18 deg)` with observed/predicted range residual 0.09 inches and bearing residual 0.00 degrees; the tag heading is 2.05 degrees from the intended 146.23-degree travel heading. Captured `recovery_route_live.log`, `recovery_route_live.mp4`, and start/end PNG frames. Disabled the one-boot route, updated the stationary anchor to the post-route tag-corrected pose, and set the rear lever to 5.18 inches for final no-motion firmware.
- `2026-07-11T00:25:10.0212273-07:00` - Audited the web UI after the user's paired-prop/circle-distribution observation. Found stale browser constants: rear lever 2.5665 inches and start pose `(-48.1,0.5,94.2 deg)` despite newer firmware calibration. Updated the UI to rear lever 5.18 and current start `(22.91,41.15,144.18 deg)`. Added `drawAiVisionHypotheses`: for every Goal sharing the observed tag ID and every camera-facing tag face, draw the measured range circle plus an uncertainty blob at the implied robot-center position. The firmware-selected Goal/face is green; alternate paired-prop/face hypotheses stay amber/dashed instead of being hidden.
- `2026-07-11T00:25:44.7659464-07:00` - Verified the live fusion channels before restarting the UI: COM8 connected, stationary fused pose `(22.93, 41.18, 144.19 deg)`, AI tag 3 range residual `0.09 in`, bearing residual `0.00 deg`, while the current LiDAR line was rejected (`lidar=none reject=fit`). Fixed the hypothesis overlay to consume `displayPose.headingRad`, the actual fused-pose field, rather than a nonexistent `headingDeg` property. Restarted only the localization-server Python processes; the replacement server reconnected to COM8. In-app browser rendering could not run because the browser service reported `No browser is available`; source and unit verification continue, but this is not counted as a fresh rendered-page proof.
- `2026-07-11T00:27:11.2791921-07:00` - Ran the complete host regression suite after the AprilTag hypothesis UI change: 16/16 tests passed. The direct `.venv\\Scripts\\pros.exe make` entry point failed because its embedded uv Python path is stale; retried through the installed module with `.venv\\Scripts\\python.exe -m pros.cli.main make`, which linked and packaged successfully. Updated acceptance to reflect the already-captured live LiDAR-unavailable/tag-reacquisition route and the passing regression/build gate. Runtime controller start-pose entry and a fresh rendered-page check remain open.
- `2026-07-11T00:29:18.1537176-07:00` - Completed the longer-test evidence audit against the raw logs rather than the prior summary. Confirmed approximately 120 inches total commanded travel across the initial and continuation captures. The initial route safely aborted on a 14.25-degree low-power turn stall; after the minimum-power/watchdog fix, the 80-inch continuation completed with no abort, `1.54 in` raw return-position error, and `4.93 deg` raw heading error. Replaced the stale workflow report and runbook, created root `report.md`, reran all 16 tests successfully, and confirmed the final stationary live pose remains `(22.93,41.18,144.20 deg)` with Tag 3 range residual `0.10 in`, bearing residual `0.00 deg`, and zero same-side encoder spread. Repository has no `.git` metadata, so no git-status evidence is available.
- `2026-07-11T00:53:38.0965172-07:00` - Ran a new slow long-duration fusion campaign and saved every capture locally without loading raw dumps into agent context. Converted `capture_pros_serial.py` to constant-memory line-buffered streaming and added `analyze_fusion_log.py`, which reports route completion, physical-rate discontinuities, correction-step bounds, reject reasons, encoder spread, and compact JSON summaries. The first attempt completed 80 inches but safely aborted when a +15-degree turn stopped 3.19 degrees short. This exposed and fixed the 2-4 degree minimum-power dead zone. The same capture also proved a stationary absolute-correction bug: a zero-encoder heuristic repeatedly reset any pose corrected more than 2 inches from the entered start. Removed that invalid reset and added regression coverage.
- `2026-07-11T00:53:38.0965172-07:00` - Verified bounded AprilTag reacquisition with LiDAR unavailable. Starting from an 8.55-inch Tag 3 innovation, normal fusion rejected it, required 12 consistent observations of the same physical Goal/face, and then converged using at most 0.75-inch position steps. A single tag range+bearing was found to be underconstrained for simultaneous position and heading, so camera heading gain is now zero: AI Vision corrects X/Y conditioned on IMU/LiDAR heading and cannot drag angle around the observation circle. The position-only trial converged to `(28.80,38.73)` with `0.08 in` innovation while preserving `155.97 deg` heading.
- `2026-07-11T00:53:38.0965172-07:00` - Completed the final slow route: six 20-inch reverse/return pairs plus two 15-degree out-and-back turns, totaling 240 inches (20 feet) and 60 degrees commanded rotation at maximum drive power 35. No abort occurred. Raw dead reckoning returned with `6.42 in` position error and `4.32 deg` heading error; Tag 3 then localized the stationary robot to `(30.18,34.70,151.65 deg)` with `0.09 in` innovation and `0.07 in` range residual. The compact analyzer found no impossible pose/heading rates and all correction steps within limits. Maximum coupled encoder spread was left 9 degrees/right 0; tightened the firmware discard threshold from 45 to 15 degrees. Final 18/18 tests pass, firmware builds/uploads, all startup motion is disabled, and the live UI server is connected on COM8. Raw log: `slow_long_route_final_live.log`; summary: `slow_long_route_final_summary.json`.
- `2026-07-11T00:58:37.9106878-07:00` - Recovered independent BRIO 101 endpoint capture. The Windows Camera app was installed but would not expose a targetable window, and direct OpenCV access failed because the existing FFmpeg publisher already owned the camera. Inspected the publisher non-destructively, found the healthy `rtsp://127.0.0.1:8554/cam` H.264 stream, and used its actual WinGet FFmpeg executable rather than the broken PATH shim. Saved `slow_20ft_endpoint_brio.png`. Compared with `recovery_route_end.png`: the fixed view visibly shows a rightward displacement and heading change after the later route, consistent with the estimator correctly reporting that the robot did not physically return to the earlier endpoint. The camera view lacks enough independently surveyed floor points for a defensible pixel-to-field homography, so it is qualitative displacement evidence rather than claimed inch-level ground truth.
- `2026-07-11T01:21:08.0446369-07:00` - Verified V5 Controller wireless programming. PROS detected the controller as COM10. The first upload attempt was correctly diagnosed as a port conflict from `lidar_bar_server.py`; stopped only those telemetry processes. `v5 status COM10` then returned the Brain system/CPU versions, and the complete hot image uploaded wirelessly to slot 1, including download-channel entry and return to pit channel. The no-startup-motion source passed 18/18 tests and rebuilt before transfer. Remote `v5 run 1 COM10` returned successfully. Raw UI telemetry did not traverse the controller link: both the custom server and an official `pros terminal --backend solo --raw` 10-second capture received zero bytes. Wireless upload/run therefore works; wireless D4/UI telemetry needs separate controller-channel support or direct Brain USB. Saved the empty diagnostic capture as `wireless_terminal_test.log` with stdout/stderr companions.
- `2026-07-11T01:39:19.1741531-07:00` - Checked the rearranged field and attempted live Tag 0 verification. Saved BRIO frames `current_field_tag0_setup.png`, `current_tag0_probe_brio.png`, and `current_tag0_probe_brio_2.png`; the center Goal is clear but the robot/Brain display is clipped at the far-left edge. Added a permanent Brain LCD diagnostic on line 4 (`AI P<port> tag=<id> VALID` or rejection reason), added regression coverage, passed 18/18 tests, and uploaded/ran it wirelessly through controller COM10. Investigated the PROS wireless terminal implementation: explicit COM10 skips the V5 wireless FIFO wrapper, while `terminal v5` enters the controller-aware path, but channel transfer still hung and returned no telemetry even after 30 seconds. A wireless Brain framebuffer capture also timed out due the 557 KB transfer. Therefore the current geometry makes Tag 0 likely visible, but onboard detection is not claimed confirmed without reading Brain line 4 or restoring direct Brain USB telemetry.
- `2026-08-23T01:55:43-07:00` - Began a new GPS/IMU/Distance motion-data campaign on the current robot. Verified GPS P7, IMU P6, forward Distance P1, drive encoders 17/18/11/13, rear tracking rotation P5, and AI Vision P8 live. Added a synchronized host CSV logger and captured a 120-second stationary baseline: GPS valid 2,000/2,000 frames with 0.0126/0.0063-in X/Y standard deviation, 0.0098-degree heading deviation, and 0.386-in median reported error; IMU standard deviation 0.0046 degrees; all encoder spans zero. P1 saw the approximately 40-in target only 13.5% of the time at confidence 6/63, so it remains fail-closed. Diagnosed one empty capture as the Brain being on its home screen, restarted the known no-startup-motion slot, and added logger idle detection. Implemented but did not flash bounded GPS fusion with the measured 6-in-right/6-in-back/90-degree-right-facing transform, and updated the host UI parser for the current P1/GPS telemetry schema. The firmware builds cleanly; supervised motion and deployment still await a clear-field confirmation.
- `2026-08-23T01:59:29-07:00` - Extended the prepared no-startup-motion firmware with a fail-closed P1 autonomous obstacle stop: valid forward returns at or inside 8 inches abort the route, returns beyond 200 mm require confidence at least 20/63, and close returns remain usable where the PROS API documents confidence as unavailable. Reverse and manual drive are unchanged. Corrected the preferred AI Vision mapping from stale P20 to live P8 while retaining runtime discovery. Updated stale hardware/control regression expectations, added current D4/GPS/fusion/obstacle tests, and passed 23/23 host tests plus a clean PROS build. No firmware was flashed and the robot did not move.
- `2026-08-23T02:46:17-07:00` - Completed the first half of the authorized multi-sensor campaign. Deployed bounded P7 GPS fusion with the measured 6-in-right/6-in-behind/90-degree-right-facing transform, P1 8-in obstacle stopping, P8 Circle21h7 detection, and corrected current hardware telemetry. A 120-s stationary baseline showed 0.0126/0.0063-in GPS X/Y standard deviation and 0.0046-degree IMU standard deviation. The P5 lateral tracker enumerates but remained mechanically stationary through rotation, so its fusion is disabled while raw telemetry remains. A full rotation sweep found GPS heading excursions up to 8.55 degrees while the IMU was repeatable. A bounded P8 scan found Goal tag 4; four-corner PnP with official camera FOV produced stable subpixel reprojection residuals.
- `2026-08-23T02:46:17-07:00` - Researched the current official Override manual, AI Vision, GPS, IMU, Distance, and printable Circle21h7 sources. The Override Goal assembly uses the medium VEX tag; measuring the actual-size vector artwork gives a 0.875-in outer black square and 1.125-in full glyph including tabs. Replaced the provisional 1.05-in scale; the earlier Tag 4 result is now 28.26-in 3D range, 25.58-in forward depth, 3.07-in camera-right, 11.62-in camera-down, 6.84 degrees right, and 24.29 degrees down. Absolute P8-to-field correction stays disabled until the camera translation and official Goal face transforms are physically measured. Sources and hashes are recorded in `reports/sensor_campaign_2026-08-23/research.md`.
- `2026-08-23T02:46:17-07:00` - Ran nine 2/5/10-in straight out-and-return trials at 10/20/35 RPM. GPS-to-nominal-encoder scale fit was 0.884748 (0.215-in RMSE), now represented as a 2.433-in effective encoder rolling diameter while retaining the physical 2.75-in wheel size. At 35 RPM, open-loop braking overshot by up to 2.03 in and returned up to 1.49 in from start; closed-loop navigation must taper rather than coast-stop. Dashboard and endpoints are in `distance_multispeed_live_01/`.
- `2026-08-23T02:46:17-07:00` - The first scaled closed-loop GPS route correctly aborted: segment 1 reached its tolerance but GPS disagreed with odometry by 4.02 in; turn 2 stalled 30.04 degrees short and the next checkpoint disagreed by 14.81 in. A subsequent isolated 15-degree out/return proved all four drive motors healthy (matched pairs, 40 C, zero faults), IMU residual 0.65 degrees, and GPS false displacement 15.63 in as its visual quality degraded. Tightened GPS RMS acceptance from 1.5 to 0.75 in, raised consistency from 3 to 5 observations, and prohibited all GPS corrections above 12 deg/s so IMU plus encoders own turns. Restored every startup-motion flag false, passed 23/23 tests, built, flashed, and ran the safe production image.
- `2026-08-23T03:00:33-07:00` - Continued the five-hour autonomous sensor campaign. A fresh 30-second stationary run captured raw GPS walking roughly 24 inches in X and 20 inches in Y while IMU and all four drive encoders remained exactly still; the fixed temporal-cluster gate rejected every observation and held the fused pose exactly fixed. A live four-leg +/-15-degree fused turn test completed with 0.088-degree final heading residual but accumulated 0.947 inches of false encoder translation, motivating suppression of center travel only when the two drivetrain sides counter-rotate. The repeat safely stalled after only 0.54 degrees and exposed a more severe first-fix bug: a stable but wrong GPS cluster teleported the estimator about 63 inches because the explicit start pose was not marked anchored. `init_pose()` now anchors immediately; current-gate replay against the walking data and a deliberately constant 63.3-inch-wrong live fix applied zero corrections and produced 0.0-inch fused drift. Added `navigation.hpp` (init, update, current pose, turn, straight go-to, stop), changed straight-line completion to a fixed finish plane, and made the competition `autonomous()` callback fail closed instead of launching the 240-inch diagnostic route. Host regression is 24/24 and the firmware builds. The Brain disconnected before the final safe upload; source flags are false and the safe binary is ready, but the stored diagnostic image still requires replacement on reconnect.
- `2026-08-23T03:18:11-07:00` - Continued while the V5 Brain remained absent from USB. Added dead-reckoning distance since absolute correction, an empirical scale/heading error envelope, and absolute-fix age to `navigation.hpp`, FUSE telemetry, the parser, and the field UI. Audited the straight-line arrival logic and fixed a real creep/overshoot fault: the controller now brakes immediately throughout its 80-ms finish-window settle period. Ran 6,750 deterministic offline trials across 6/12/24/48/84-in paths and powers 20/40/60. At 48 in/power 40 the measured encoder/IMU envelope produced 1.63-in 95th-percentile physical error, but deliberate unobserved slip/push produced 6.76 in while the estimator claimed about 0.67 in, confirming that software cannot observe lateral pushes with P5 disabled. All 25 host tests pass and the ARM build succeeds; `bin/hot.package.bin` SHA-256 is `29fe13c490a0f7921a32075e5060a5f600bcb9a26bf52273c7af0294cd27e5f2`. A fresh Brio frame showed the robot upright with field objects present, so no motion was attempted. The on-device diagnostic image remains unsafe until the Brain cable is reconnected and this safe binary is uploaded.
- `2026-08-23T03:41:44-07:00` - Found and fixed a critical 90-degree GPS coordinate-frame defect while the Brain remained physically absent. Official VEX documentation and the captured native sample confirm VEX +X is field-right, +Y is toward the 0-degree wall, and heading is clockwise from that wall; the project uses +X toward that wall, +Y red/left, and counterclockwise heading. Added one central `gps_frame.hpp` conversion (`project_x=native_y`, `project_y=-native_x`, `project_heading=-robot_cw`) and routed the estimator, GPS route diagnostic, wall recovery, and both host replays through it. A compiled cardinal-axis test plus a real stable frame gives 150.36 degrees versus the independently entered 151.65 degrees; the old expression gave 240.36 degrees. Corrected replays still apply zero GPS corrections: the walking failure peaks at 27.85 inches and the stable wrong fix at 16.24 inches, with 119 quality + 131 settling rejects and 121 position-innovation rejects respectively. The earlier 71.7/63.3-inch figures were artifacts of comparing mismatched frames and are superseded in the qualification report.
- `2026-08-23T03:41:44-07:00` - Directly verified the official Override v1.1 Goal tag numbering and corrected the nonzero ID table to `4,3,1,2 / 2,1,3,4` around center ID 0; coordinates/colors were already correct and AI correction remained disabled, so the stale IDs never moved the robot. Expanded the navigation shadow sweep from 6,750 to 90,000 deterministic trials (2,000 per cell; 24-MB CSV, SHA-256 `d0083daf8f1d66293f40a3fc32bdc8b223305e39f87b0d548240f8e3c4150678`). At 48 in/power 40 the measured envelope remains 1.63-in p95 / 1.82-in max and 100% within 2 in; hidden slip/push is 7.04-in p95 / 8.31-in max while the estimator reports 0.67-in p95. At 84 in/power 40, only 80.45% of measured-envelope trials remain within 2 in.
- `2026-08-23T03:41:44-07:00` - Ran 20,000 P8 PnP sensitivity trials over six unique live Tag 4 corner quads. With explicit ±0.5-px corner, ±1-degree FOV, ±1-px principal-point, and ±1% print-scale what-if bands, the 95th-percentile absolute change is 2.15 inches horizontal range, 0.32 degrees bearing, and 0.52 degrees elevation. This is sensitivity evidence, not calibration; unknown lens distortion and motion blur remain. P8 field correction stays disabled pending tape-measured multi-range calibration and physical extrinsics. Host regression is now 26/26. The Brio enumerates but produces fully black frames even after a three-second exposure warm-up, and no `/dev/ttyACM*` or VEX USB device has reappeared, so no blind motion or upload was attempted.
- `2026-08-23T03:46:04-07:00` - Audited active-command sensor loss and P8 temporality while hardware remained disconnected. Losing either trustworthy drive-encoder side now latches the pose invalid, stops blocking motion, and requires explicit `navigation::init()` after recovery instead of integrating across an unobserved gap; losing the IMU likewise invalidates public motion state and aborts the command. P8 fusion now consumes each `poll_id` at most once, so a cached shadow snapshot cannot satisfy several temporal-confirmation observations. All startup-motion flags remain false, 28/28 host tests pass, the ARM image builds, and `bin/hot.package.bin` SHA-256 is `7b5e8863da7a7131097a87d79fb6195bd2dfff9a01084bb1392e0d4a36259a69`. No `/dev/ttyACM*` or VEX USB device was present and the Brio remained black, so no blind motion or upload was attempted.
- `2026-08-23T03:47:16-07:00` - Tightened drive-side validity from “at least one finite encoder” to all configured motors responding. On this two-motor-per-side chassis, one unplugged motor can no longer disappear from the average and allow asymmetric autonomous motion to continue. The official PROS motor-position API returns an error sentinel on failed reads, so the exact expected-count gate rejects that case before motion. The revised safe image passes 28/28 host checks and builds with SHA-256 `b765051e94cf09a3ac2293c99ce7a5314985c7d6af4a4db7714e63bc1258a91d`; the Brain is still absent from USB.
- `2026-08-23T03:49:02-07:00` - Found and fixed a P1 fail-open edge case from the official PROS Distance contract: `PROS_ERR` is a large positive integer, so the old negative-only fault test could treat a failed API read as clear. Forward autonomy now aborts on a missing sensor, failed distance/confidence call, or out-of-contract confidence, while preserving 9999 mm as the documented healthy/no-target result. GPS and AI health also retain the last evaluated physical-poll result instead of being overwritten between polls. All 28 host checks pass and the ARM image SHA-256 is `392afd5ba73d9fe2b5c5c6c8fcfc5bf4b9b5c01989dd332231e05489c77efc25`.
- `2026-08-23T03:49:58-07:00` - Tightened live P6 validation using the official PROS IMU contract. A heading sample now requires the device to be installed, not calibrating, and not in API-error status before its finite rotation value is considered. Any failure latches public pose invalid and aborts blocking motion until explicit reinitialization. The no-motion ARM image passes 28/28 checks and now hashes to `d0012decb17e8cbf35a96b07c6200840703dbf4fe1eacedb48d4dc43c0aec4b6`.
- `2026-08-23T03:52:20-07:00` - Added a deterministic GPS outage/return replay using the production 10-Hz, 12/30-poll, 3/6-in, 0.5-in-step gates. Measured-envelope errors of 1.34 and 2.68 in begin correction at 1.1 s and fall below 0.5 in at 1.5/1.8 s. A 5.35-in disagreement waits for the 30th poll (2.9 s) and falls below 0.5 in at 4.1 s. The 7.04-in hidden-slip p95 case remains rejected for the full replay, proving a returned GPS fix cannot teleport the estimator after a displacement outside the recovery envelope. CSV SHA-256 is `381466ae507e5a2c821c49b59b595ce5affa34d241e0e12774336d0bdc8de9bb`.
- `2026-08-23T03:53:07-07:00` - Diagnosed the independent Brio 101 supervision camera across raw YUYV and MJPEG at 320x240, 640x480, and 1920x1080 after four-second warm-ups. Every mode is black; YUYV is exactly studio-black (`YMIN=YAVG=YMAX=16`), 640x480 MJPEG averages luma 2.00, and 1080p averages 2.03 with only a tiny hot pixel. This rules out a single codec/resolution failure. Logitech's official Brio 101 guide documents an integrated physical privacy shutter that must slide fully right, making a closed/covered shutter or unlit lens the leading physical cause. The V5 Brain remains absent, and no motion was attempted without visual supervision.
- `2026-08-23T03:54:18-07:00` - Made the P7 stopped-robot contract explicit. The fixed 0.75-in temporal cluster cannot distinguish one location from legitimate moving coordinates, so GPS consistency now resets above 0.5 in/s encoder speed in addition to the existing 12-deg/s rotation gate. This matches VEX's recommendation to pause for accurate fixes: P6 plus drive encoders own moving legs, while deliberate brief stops allow bounded GPS correction. All 28 host checks pass, all startup-motion flags are false, and the safe ARM image SHA-256 is `fe66a2eaf0d461f9d51e28bbac26a1fdc5159b1776c9e113fb076b8b7b6f75fc`.
- `2026-08-23T03:57:05-07:00` - Audited the P8 algorithm/report boundary. The Brain currently computes an opposite-edge/focal approximation and emits four corners; OpenCV planar PnP, plane normal, and reprojection residual are offline reanalysis, not onboard firmware. Across 15 captured Tag 4 frames, onboard horizontal range was 25.559 in median versus 25.763 in offline PnP; PnP minus onboard was +0.205 in median and 0.968 in worst absolute difference. Corrected qualification/research/API documentation to make this explicit. P8 field correction remains disabled because one-view agreement does not replace calibrated intrinsics/distortion, physical mount extrinsics, multi-range tape validation, or live face orientation.
- `2026-08-23T03:57:44-07:00` - Completed the second P8 temporality fix. Each poll ID was already limited to one count, but an old count could survive fresh no-tag, stale, bad-range, unmapped, or ambiguous observations. Every failed fresh poll now resets consistency, so only consecutive accepted Goal/face observations can reach the normal or reacquisition thresholds. Field correction remains disabled. All 28 host checks pass, the safe ARM image builds, and SHA-256 is `72d071f993bc45a8f5385baf91cd9c5948b57eb3b9435c93067f71e5e6e141e3`.
- `2026-08-23T03:58:29-07:00` - Added bounded straight-line stall protection. Once meaningful forward power is present, failure to gain 0.10 in of raw drive-encoder travel for 1 s stops all drive motors and reports `drive_stall` instead of pushing until the 12-s route timeout. The watchdog deliberately uses raw encoder travel, not fused X/Y, so a GPS/AI position correction cannot fake mechanical progress. Wheelspin remains unobservable with P5 disabled. The suite is now 29/29 and safe ARM SHA-256 is `d5d02580c4e43b8ca683a81d29cafee89100240e001cc0a14a1d10c29b49ffba`.
- `2026-08-23T03:59:47-07:00` - Fixed the turn arrival-settle controller to brake immediately throughout the 100-ms heading confirmation window rather than continuing to apply PD power after entering tolerance. If inertia carries the heading outside tolerance, normal correction resumes. All 30 host checks pass and safe ARM SHA-256 is `621bebf0abdb3f060b329dc35551a7063de18dfa696c96f74ffeb0af43763530`.
- `2026-08-23T04:01:19-07:00` - Generated a 700,000-sample random scheduler-phase fault-response matrix (100,000 phases for each of seven periodic events). Code-loop upper bounds are 20 ms for drive-encoder/IMU/P1 detection, 100 ms for GPS/P8 rejection, and 1.02 s for a mechanical no-encoder-progress stall. Correct GPS return remains deliberately gated at 1.1 s normally or 2.9 s for 3-6-in reacquisition; lateral slip/push is explicitly unobservable with P5 disabled. These are software timing bounds, not measured Smart Port latency, coast, or physical braking distance. CSV SHA-256 is `103fefe74235b4c936a9c1b0e6d449f9d70bdfe53720ca2fec5831759a736d9a`.
- `2026-08-23T04:02:38-07:00` - Strengthened P8 freshness beyond estimator-side poll IDs, which are API polls rather than true optical-frame timestamps. The estimator now anchors each changed corner geometry to cumulative raw drive travel and P6 heading. Exact repeated corners remain acceptable while stationary, but are rejected as `stale_geometry` after more than 0.5 in independent encoder travel or 2 degrees heading change without a pixel-corner change. All 30 host checks pass, all motion flags are false, and safe ARM SHA-256 is `8f533197027d49b68c0049731f8842528400ed0b4cadeeac044085a2744a89a9`.
- `2026-08-23T04:04:09-07:00` - Expanded the public navigation sensor-health contract without enabling unqualified corrections. P1 now exposes API health separately from target validity, so healthy 9999-mm/no-target and `PROS_ERR` are distinguishable. P8 exposes tag ID/visibility, horizontal and 3D range, right-bearing, elevation, geometry age, and estimator rejection state. All 30 host checks pass and safe ARM SHA-256 is `c6d1c5f4fa756958653c7ee56d700a695f5396e559c0aaf333b68d1d7320cdb1`.
- `2026-08-23T04:05:19-07:00` - Found stale prior-robot P8 extrinsics in the host field UI (`+6.75 in forward, -10.44 in right, 90 deg yaw`) despite the replacement firmware correctly using unmeasured neutral values. Reset the UI to `(0,0,0)`, added `aiCameraExtrinsicsQualified=false`, suppressed the otherwise convincing but wrong field-position hypotheses, and display `mount UNMEASURED`. All 30 tests pass; firmware hash is unchanged because this is host UI only.
- `2026-08-23T04:08:31-07:00` - Audited the browser fallback and public navigation initialization boundary while the Brain remained absent. The UI was still converting encoder rotations with the nominal 2.75-in wheel, overstating fallback travel by about 13%; it now uses the measured 2.433055-in effective rolling diameter and explicitly disables the mechanically inactive P5 side tracker. Found a safety/API hole where `turn_to()` or `go_straight_to()` could implicitly initialize from background dashboard telemetry without a successful caller-supplied `navigation::init()`. Added a separate motion-authority latch: dashboard telemetry remains available, but public pose validity and movement require explicit successful initialization, and changing/resetting the start pose revokes it. All 30 tests pass, all motion flags are false, and the safe ARM image SHA-256 is `76c78ba8ae5ad613f1aedb2e35392ee70f03c1d0c254c50a2fa38e43f9895337`. No VEX USB device was present; no upload or motion was attempted.
- `2026-08-23T04:19:13-07:00` - Continued the disconnected-hardware audit. Closed the 201-203-mm P1 stop-boundary gap: any physical return at or inside 8 in now stops regardless of confidence, non-9999 values outside the official 20-2000-mm range fail closed, and 9999 remains the documented clear/no-target value. Found that the shared autonomous stop helper used zero-voltage `motor.move(0)` despite describing braking; it now cancels the chassis command, configures brake mode, and calls the PROS `brake()` operation. Added a 250-ms navigation-init stationarity gate (each drive side <=0.10 in, P6 <=1 deg), made `sensor_health()` a read-only preflight query that cannot reset P6 or create a fused pose, and made the host dashboard re-enumerate a serial port after 6 s without recognizable telemetry. Generated `braking_evidence_dashboard.png`, which honestly separates the old measured coast overshoot (2.032 in worst through 35 RPM) from the still-unmeasured new physical brake distance and P1 lens-to-bumper offset. A new two-solution IPPE analysis of six unique P8 Tag 4 corner quads found only 0.043-in maximum horizontal-range disagreement and 0.033-px reprojection-gap disagreement, but 41.7-68.6-deg 3D face-normal ambiguity; P8 field correction remains disabled. All 31 host tests pass, all motion flags are false, and the safe ARM image SHA-256 is `9be82f960b66dfa8441966153b2f675f58bbd5d6721af77ed33baac1e35df770`. The Brain remains absent, so the safe image is not yet uploaded and no motion was attempted.
- `2026-08-23T04:27:50-07:00` - Continued the authorized five-hour campaign with the Brain still absent. Added motion-anchored P7 freshness: an exactly repeated GPS tuple after more than 0.5 in drive motion or 2 degrees P6 change is rejected as `stale_geometry`. A deterministic cached-pre-move replay shows the prior temporal gates could accept at 2.1 s and induce 1.7316 in error, while the new gate induces 0.0 in error and accepts the first changed tuple only after 12 fresh polls. Fixed the capture logger so GPS internal gyro Z is parsed as a float, NaN/inf fault sentinels survive parsing, and actual D4 sensor frames rather than arbitrary serial lines control idle timeout. Generated `gps_stale_cache_dashboard.png` and brought host regression to 32/32.
- `2026-08-23T04:27:50-07:00` - Quantified the authoritative stationary P6 trace with a reproducible integrated-heading analysis. Across 2,000 samples/119.96 s at 16.67 Hz, P6 reported only 0.00 and 0.01 degrees: 0.010-degree peak-to-peak, 0.004631-degree standard deviation, and a descriptive linear trend of 0.000442 degrees/min (0.000884 degrees over the run), while all four drive-encoder spans were exactly zero. Generated `imu_stability_dashboard.png`, JSON, and two-sample heading-deviation CSV. The report explicitly does not claim absolute accuracy, raw-rate Allan deviation, temperature performance, dynamic accuracy, or long-term drift. Also removed zero-voltage coast stops from the legacy fused diagnostic route so its arrival/timeout/final paths use the same brake helper. All motion flags remain false; the rebuilt safe image passes 32 tests and hashes to `95af2ba976deccc6f002b34c7bc91b968945287434f59aab5d6fe435f6975626`. No VEX USB device or `/dev/ttyACM*` exists, and the Brio remains unusable/black, so no upload or blind motion occurred.
- `2026-08-23T04:32:03-07:00` - Found and fixed a coupled drivetrain-calibration defect before reconnect. The live 12.0086-in effective track had been calibrated while encoder travel used the physical 2.75-in wheel model; later applying the 0.884748 straight-distance scale only to wheel diameter would under-report encoder-derived turns by 11.5%. The same factor is now applied to effective track, producing the ratio-equivalent pair 2.433055-in wheel / 10.624582-in track in firmware and the browser fallback. Reanalysis of five live 15-90-degree outward turns shows the paired model retains 1.224-degree RMSE versus P6, while wheel-only scaling would produce 7.516-degree RMSE and 12.179-degree maximum error. Generated `drive_geometry_scale_consistency_dashboard.png` (SHA-256 `5f5025655115bb55d4ae6e53bf93872bd03ee1639aba109e928158d1c17908a4`) and documented that 10.624582 in is an effective encoder-model parameter, not physical chassis width. All 32 tests pass, all motion flags are false, and the rebuilt safe image hashes to `55c9c95193fed844d14f5c7f1fab7a2a633cb06c4a37a5bf01a9c273a1811fec`. The Brain remains absent, so it has not been flashed and no motion occurred.
- `2026-08-23T04:35:05-07:00` - Added a fail-closed map precheck to the public straight-line API. Before it issues either the initial turn or forward command, the requested center-line segment must clear all nine official Goal centers and its endpoint must lie inside a provisional 14-in wall inset. Normal Goal-center clearance is 16 in; a legal start already near a Goal may move outward through a bounded escape rule with a 12-in floor. Rejection returns `kUnsafePath`, brakes, and logs the reason/clearance. Official Override v1.1 permits an 18-in starting robot and up to a 24×24-in match footprint, but those limits do not establish this robot's swept radius when its rotation center is offset or a mechanism expands. Documentation therefore requires physical front/back/left/right/expanded-envelope measurement and notes that forward-only P1 cannot protect side/rear collisions during an in-place turn. All 32 tests pass, all motion flags remain false, and the safe ARM image hashes to `dec19b6237e3740b99f1157720413bc1cf5ad957eb275892d5510dc17f65c47a`. The Brain is still absent, so nothing was uploaded or moved.
- `2026-08-23T04:37:08-07:00` - Stress-tested the new public route gate over 250,000 deterministic uniformly random field segments. It accepted 17,032 (6.81%), rejected 143,329 first for Goal clearance and 89,639 first for endpoint wall clearance, with zero accepted wall/Goal invariant violations. The low direct-path acceptance rate reflects nine dense mapped Goals and shows that general point-to-point navigation needs a separately validated waypoint planner; this run proves only gate logic and does not model footprint, expansion, scoring objects/robots, controller deviation, overshoot, pose error, or turn sweep. Generated `route_safety_stress_dashboard.png` (SHA-256 `1afea047ad1ce637c75ef1f798886eb6ddf6f1a9aaa7123e84c394ba45acf9c8`). Also fixed the public `go_straight_to()` timeout contract: the initial turn and drive now share one end-to-end deadline instead of each receiving nearly the full allowance. All 32 tests pass, startup motion remains disabled, and safe ARM SHA-256 is `8553328f3e3ecfa30a5ee8af9ee71b7542d5a06c240f9626d831c4b6ae07335a`. The Brain remains absent; no upload or motion occurred.
- `2026-08-23T04:42:48-07:00` - Completed deeper P1 and P7 policy audits. P1 produced physical ranges in 270/2,000 stationary frames (13.5%), split into 237 bursts with 0.06-s median/0.18-s maximum duration; the roughly 40-in returns spanned 6.22 in and all had only 6-8/63 confidence, so zero were precision-usable. It returned no physical target in 860 rotation-capture frames. `p1_reliability_dashboard.png` (SHA-256 `df92de304ffcde7dbca3a54ea28a6cd409da451e66a8885659e0fcf475c1c0e4`) reinforces P1's role as close fail-closed stop only; close-target truth, latency, bumper offset, and brake distance remain unmeasured.
- `2026-08-23T04:42:48-07:00` - Replayed five live stopped-orientation P7-vs-P6 discrepancies through the old heading policy. Only the +15-degree view passed quality and the 5-degree heading gate, but its repeated bounded updates still accumulated 2.961 degrees of bias against P6 (largest step 0.342 degrees). Set P7 heading gain and maximum heading step to zero while retaining bounded position correction; accepted position fixes now report `position_only`. Added `gps_gyro_valid` so PROS's infinity error sentinel cannot be mistaken for a GPS gyro measurement. `gps_heading_policy_replay_dashboard.png` SHA-256 is `a97059129c582a13544a65ca76d0b9b957002d1e86065c32e7812029c9dbbaac`. All 32 tests pass, all startup motion flags remain false, and safe ARM SHA-256 is `e364b0a14044171545e98cb40c25ce37749747d61665d33ebaa0f060a98789e1`. The Brain remains absent; no upload or movement occurred.
- `2026-08-23T04:48:46-07:00` - Audited the current robot against stale four-LiDAR code from the previous chassis. Production defines only the port-1 forward Distance device; the legacy wall-fit function immediately returns unavailable and never reads ports 6-9, so it cannot contend with the current P6 IMU, P7 GPS, or P8 AI Vision devices. Added a heap-free 512-point navigation path ring with session time, error envelope, absolute-fix age, newest-suffix copying, and explicit clearing/reset behavior. The trail follows the same encoder+P6 estimate during absolute-sensor outages and is therefore estimator history, not ground truth. After the final clear/reset edit, 32/32 host tests pass, the ARM build succeeds, startup motion flags remain false, and `bin/hot.package.bin` SHA-256 is `998fbcf484a8f75d14ffcc102d6581ea6afaab8be6d9aa9dd3f84c3b30e97b07`. The Brain is still absent and the Brio still black; no upload or blind movement occurred.
- `2026-08-23T04:55:32-07:00` - Quantified the four independent drive-motor encoders across every saved current-robot telemetry log. The five runs with at least 5 motor-degrees of travel contain 9,798 same-side samples; absolute P17/P18 or P11/P13 spread was 3.0 motor-degrees at p95 and 6.0 degrees maximum, with zero samples over the 15-degree fail-closed cutoff and 9 degrees of margin to the worst live case. Generated `drive_pair_consistency_dashboard.png` (SHA-256 `ca5b8f10bed49edb20121f8b8724a1f5a3d94b144106e55294e328d5cb23e38d`), SVG, CSV, and JSON from a reproducible analyzer. The gate can catch one missing/stalled/asymmetric coupled motor, but equal wheelspin remains unobservable. Official GPS research also confirmed that VEX distinguishes direct, extrapolated, heading-only, and frozen fixes with a quality percentage that PROS does not expose; the firmware's independent RMS, fixed-cluster, motion, innovation, and stale-tuple gates remain necessary. Host regression remains 32/32; firmware source and hash are unchanged. The Brain has not reconnected.
- `2026-08-23T04:58:44-07:00` - Evaluated general Goal-avoiding waypoint geometry without deploying it. Among 3,000 deterministic random pairs whose endpoints already satisfied the 14-in wall inset and 16-in Goal-center exclusion, only 36.33% had a safe direct segment; a 2-in A* grid plus exact line-of-sight smoothing found a route for 100%. Indirect routes had 1.047x median, 1.235x p95, and 1.697x maximum distance stretch, with four total points median and six at p95. Generated `waypoint_planner_dashboard.png` (SHA-256 `3d3a64e0ddc9c6bc02f8ba109b292ef2dc16eb46bd1a1f55caaafc863e0a82e0`), SVG, 3,000-row CSV, and JSON. The earlier 6.81% direct acceptance included randomly invalid endpoints; 36.33% is the legal-endpoint baseline. This remains host-only because the current/expanded footprint, turn sweep, pose-error inflation, controller deviation, movable Blocks, and other robots are not modeled. The Brain remains absent, so no upload or motion occurred.
- `2026-08-23T05:04:18-07:00` - Audited disabled motion diagnostics and found two routines constructing duplicate P7 GPS/P6 IMU objects plus one copied approximate Goal map. They now reuse the single shared `gps_7` and `chassis.imu` owners and iterate the authoritative `kGoalTagLandmarks` and shared clearance constants. Expanded the regression invariant to discover every startup/competition motion flag by regex and require all of them false, rather than checking only five named flags; also added a single-owner GPS/IMU test. The suite is 33/33, the safe ARM image builds with only vendored LVGL warnings, all motion flags remain false, and SHA-256 is `aec22864904a5e453b6b4345f9f59bf2251b97f64973a2216106e620a0e12a8f`. Brain USB and visual supervision remain absent, so it was not uploaded or run.
- `2026-08-23T05:08:11-07:00` - Removed heap allocation from the fresh-P8 path inside the 50-Hz estimator. AprilTag IDs 1-4 map to at most two physical Goals and four faces each, so association now uses a fixed eight-element `std::array`, a bounded candidate count, and a fail-closed overflow state instead of constructing/sorting a `std::vector` on every optical poll. The math and gates are unchanged, but allocator latency/fragmentation is eliminated from the control loop. Added a regression invariant for the fixed-storage contract; 34/34 tests pass, the safe ARM image builds, all motion flags are false, and SHA-256 is `852920be356ab45ac2a70f0be28e965c9744f0ffb0dcdcbaafe0fa5585f3799e`. The Brain remains disconnected, so no upload or movement occurred.
- `2026-08-23T05:12:36-07:00` - Audited the browser dashboard against the current hardware. It still presented its original `LiDAR Ports 6-9`/`4x LiDAR` title and labeled P1 as one element of a four-sensor wall array even though the old wall bar is absent and current ports 6-8 are IMU/GPS/AI Vision. Renamed it to the multi-sensor localization dashboard, labeled P1 as the forward Distance sensor, and made the wall-range/angle panel explicitly say the legacy four-sensor fit is disabled rather than waiting forever for missing points. Added a regression check that rejects the stale titles. Python compilation and 35/35 host tests pass; the firmware hash is unchanged at `852920be356ab45ac2a70f0be28e965c9744f0ffb0dcdcbaafe0fa5585f3799e`. Brain USB remains absent and the Brio remains black.
- `2026-08-23T05:16:24-07:00` - Aligned browser fallback odometry with the production encoder-failure contract. The UI previously averaged whichever motor remained on a coupled side, retained a plausible-looking pose across an encoder gap, and integrated tiny gearbox mismatch as translation during pure turns. It now requires both motors per side within the 15-degree spread gate, reports each pair's spread, latches fallback invalid after loss, requires an explicit justified start-pose reset after recovery, and suppresses center translation when the two sides counter-rotate. This changes only diagnostic rendering, not onboard fusion. Python and extracted JavaScript syntax checks plus 36/36 host tests pass; safe firmware remains SHA-256 `852920be356ab45ac2a70f0be28e965c9744f0ffb0dcdcbaafe0fa5585f3799e`. The Brain remains absent.
- `2026-08-23T05:20:07-07:00` - Closed a stale-validity presentation gap between firmware and the field UI. `FUSE_TEST` now emits `pose_valid=1` only while both coupled drive sides and P6 are valid. The parser retains the bit, the browser uses an onboard pose only when it is explicitly valid and fresh, and an invalid fresh frame shows a prominent warning instead of a frozen tournament pose. The independent browser fallback remains diagnostic and obeys its own coupled-encoder latch. Python, extracted JavaScript, 37/37 host tests, and ARM build pass; all motion flags are false and safe image SHA-256 is `6239b4a6f6fc4ee1d8a54d39a9b010b3c11129d90e3995ef50de66b76c81915a`. The Brain has not reconnected, so this image is not uploaded.
- `2026-08-23T05:23:42-07:00` - Made the public 512-point path-history API task-safe. A logger/UI task can call `path_size()`, `copy_path()`, or `clear_path()` while the 20-ms estimator records motion; those operations previously raced over ring indices and point contents. Reset, append, copy, size, and clear now hold one small PROS mutex only for the bounded memory operation; pose math, sensors, and motor control remain outside it. Nested reset paths use an explicitly unlocked helper to avoid self-deadlock. Added the concurrency invariant; 38/38 tests and ARM build pass, all motion flags are false, and safe image SHA-256 is `ea871d8201ee415f82bdc1f9b374f153f2fd966061fadbb27507df3c9460f519`. The Brain remains absent, so no upload or movement occurred.
- `2026-08-23T05:27:16-07:00` - Quantified the P1 emergency-stop geometry while live close-target testing remained unavailable. At the configured 8-in lens threshold, subtracting VEX's quoted +/-15-mm close-range error leaves 7.409 in for the sum of lens setback behind the front bumper and physical travel after detection. If the old pre-brake 2.032-in coast overshoot repeated, lens setback would need to be at most 5.377 in merely to retain nonnegative bumper clearance. Generated `p1_stop_margin_dashboard.png` (SHA-256 `984a806948c1400cbb2f27c5a20cd7279eb277f0666c39c5c740f016114ecce1`), SVG, CSV, JSON, and a reproducible analyzer. This is sensitivity, not collision certification: current setback, new brake-mode stopping distance, real acquisition/control latency, material, and incidence angle are still unmeasured. Firmware remains unchanged at SHA-256 `ea871d8201ee415f82bdc1f9b374f153f2fd966061fadbb27507df3c9460f519`; Brain USB is still absent.
- `2026-08-23T05:07:39-07:00` - Split estimator health from authorized field-pose validity in telemetry and the browser UI. `estimator_valid=1` now means the coupled drive encoders and P6 are healthy enough to propagate the background estimate; `pose_valid=1` additionally requires the separate `navigation_api_initialized` authority established by a successful stationary `navigation::init()`. The dashboard uses only fresh `pose_valid=1` data as its tournament pose and gives distinct invalid-estimator versus uninitialized-pose warnings. Python, extracted JavaScript, 38/38 host tests, and the ARM build pass; all motion flags remain false and the new safe image SHA-256 is `a191635f43323f0fbd222f14fa5aa9596f9de89dd0f4313c25c27e5d7a36e290`. The system clock reported an earlier wall time than the prior journal entry; the timestamp is recorded verbatim. Brain USB remains absent, so this image has not been uploaded and no movement occurred.
- `2026-08-23T05:11:51-07:00` - Converted the production dead-reckoning envelope into explicit GPS-outage distance budgets. The measured 2.1554% encoder-scale envelope combined with the 0.91-degree heading envelope produces 2.677% position-error growth per inch of reported travel. From a representative 0.39-in last-fix base, 1/2/3-in total budgets allow 22.8/60.1/97.5 in of additional dead reckoning; from the 0.75-in GPS RMS gate limit, only 9.3 in remains before a 1-in budget is exhausted. Generated `gps_outage_distance_budget_dashboard.png` (SHA-256 `4294dc926cd6ed9e0eddea9f4d5cdb96c3475161844af86990d6e21764f86c9c`), SVG, CSV, JSON, and a reproducible analyzer. The chart explicitly excludes slip, pushing, collision, initial placement, and long-term/temperature IMU drift. Strict ARM `-Wall -Wextra -Wshadow -Wconversion -Wsign-conversion` syntax audit found no production estimator/navigation warnings; project warnings were limited to disabled RPM calibration diagnostics. Brain USB remains absent and no movement occurred.
- `2026-08-23T05:13:56-07:00` - Closed an uncovered turn-sweep safety path. The public straight-segment gate constrained endpoints and Goal crossings, but `turn_to()` and the initial turn inside `go_straight_to()` could rotate beside a wall or Goal even though P1 sees only forward. Meaningful public turns now require the center to satisfy the provisional 14-in wall inset and 16-in mapped-Goal clearance; an already-aligned robot may still drive an outward escape segment without rotating. This remains a center-point proxy pending physical current/expanded swept-footprint measurement. Added a regression invariant; 39/39 host tests and ARM build pass, all motion flags are false, and safe image SHA-256 is `81ca27255dd0f0f73032d3c17745c0c36f6aea003a7cbac6fffc50c146446c13`. The Brain remains absent, so this image is not uploaded and no physical turn was attempted. A fresh Brio frame was also essentially black (Y 17-18/255), so blind motion remains prohibited.
- `2026-08-23T05:15:59-07:00` - Fixed P8 runtime reconnect recovery. After a hot unplug, a negative AI Vision object-count read had left `installed/configured` latched true, so every later poll returned `count_error` and the existing one-second rediscovery path was never entered. Read failure now marks configuration invalid, distinguishes a still-enumerated read error from an absent sensor, and forces bounded reinitialization; every initialization also clears corner geometry cached before the reset/unplug. The existing retry test now covers these transitions. All 39 host tests and the ARM build pass, every motion flag remains false, and safe image SHA-256 is `b4ab36b6a57e63f567364d86bb15110e2b4c32c4541b9db09769dd9003a63bd5`. Brain USB remains absent, so the fix cannot yet receive a live P8 unplug/replug acceptance test or upload.
- `2026-08-23T05:18:41-07:00` - Updated the 250,000-command route stress model to match the new initial-turn gate. Each deterministic trial now includes a random starting heading; rejection order mirrors production: endpoint wall, segment Goal, turn-center wall, then turn-center Goal. The segment-only acceptance remains 6.812%, while the full command gate accepts 9,970/250,000 (3.988%); 4,280 additional commands are rejected for turn Goal clearance and 2,782 for turn wall clearance. Accepted commands have zero wall, Goal-segment, or required-turn invariant violations. Regenerated `route_safety_stress_dashboard.png` (SHA-256 `eb0c67a218aa3071d33d591622bdd5e382786c933425ec591f68414a9db1e11d`), SVG, examples CSV, and JSON. This validates software gate logic only: actual swept footprint, mechanisms, moving game pieces/robots, controller error, and pose uncertainty remain unmodeled. Firmware is unchanged at safe SHA-256 `b4ab36b6a57e63f567364d86bb15110e2b4c32c4541b9db09769dd9003a63bd5`; Brain USB remains absent.
- `2026-08-23T05:19:38-07:00` - Improved P8 availability when several AprilTags share a frame. The old loop selected the largest raw tag and only afterward applied clipping, convexity, edge, aspect, fill, and area gates, so a large invalid border tag could hide a smaller fully usable tag. Selection now tracks both the largest raw diagnostic candidate and largest geometrically usable candidate, preferring the latter; invalid geometry is surfaced only if no usable tag exists. Added a regression invariant. All 40 host tests and ARM build pass, all motion flags remain false, and safe image SHA-256 is `be18bbdabf2eb2324d49b5ee5842d6084007f7371d8228e0b1a610454ef73d09`. Brain/P8 USB remains absent, so multi-tag live acceptance and safe upload are pending.
- `2026-08-23T05:20:30-07:00` - Ran the strict ARM conversion/shadow warning audit on the revised P8 path and removed its sole project warning by using the AI Vision API's unsigned object index type after the already-bounded nonnegative count check. The strict pass now reports no P8 project-source warnings; the normal build still shows only the vendored LVGL enum warning. All 40 host tests pass, all motion flags remain false, and the rebuilt safe image SHA-256 is `21832c0d83c487dcf2210e739f8bea289cb498673c008ec73118f67d5b57b548`. Brain USB remains absent, so it is not uploaded.
- `2026-08-23T05:21:31-07:00` - Closed a stale browser-fallback display gap. Onboard fused telemetry already required freshness, but the independent D4 encoder fallback could remain `ready` forever after serial loss and display a plausible frozen pose. The browser now requires the server connection plus a D4 `pc_time` no older than 1.5 s; otherwise readiness is false, the label says `STALE`, and the detail explicitly calls the frozen path diagnostic history. Python parser tests and extracted JavaScript syntax checks pass (40/40 host tests). This is host UI only, so safe firmware remains SHA-256 `21832c0d83c487dcf2210e739f8bea289cb498673c008ec73118f67d5b57b548`. Brain USB remains absent.
- `2026-08-23T05:22:03-07:00` - Verified P7 field availability against the current official Override v1.1 manual. RSC4 requires VEX GPS Field Code for Autonomous Coding Skills, but T7 lists strips as a permissible event-selected modification and T21 warns Head-to-Head and Skills fields may differ, including Head-to-Head fields without strips. Therefore normal match autonomous cannot depend on P7 availability; the explicit start anchor plus encoder/P6 dead reckoning is mandatory, and P7 remains opportunistic bounded correction. Added this rule-level constraint to research, navigation, and qualification docs. Firmware behavior already matches it and is unchanged at safe SHA-256 `21832c0d83c487dcf2210e739f8bea289cb498673c008ec73118f67d5b57b548`. Brain USB remains absent.
- `2026-08-23T05:26:09-07:00` - Confirmed the current Override v1.1 Appendix A itself contains the Goal AprilTag numbering/location specification, unlike optional Head-to-Head GPS strips. Saved VEX's 1110x1301 source drawing as `official_override_apriltag_locations.png` (SHA-256 `694a882f72a7d89260241334b9ee9157d6b0936cb251b76ba879dfc8a43dec02`) and documented the distinction: Goal tags are nominal static landmarks, but visibility still is not guaranteed. Also closed a navigation-library task race: `current_pose()`, fused `sensor_health()`, and concurrent path clearing now read a mutex-protected complete estimator snapshot, while sensor/motor I/O remains outside the lock and motion calls remain single-owner. 41/41 host tests and the ARM build pass, all motion flags are false, and the new safe image SHA-256 is `270e2fc4214336a3d8ec5995a22dcac8083cffc0751ab3d11179e2f9424e59b4`. Brain USB remains absent, so this image is not uploaded and no movement occurred.
- `2026-08-23T05:38:40-07:00` - Hardened concurrent read and stop behavior. P8 now publishes a whole mutex-protected optical frame instead of returning a reference to fields being rewritten. Estimator/path/P8 reads are task-safe while init/update/motion remains single-owner. `navigation::stop()` now latches an atomic cancellation checked by public blocking loops, connected field disable follows the same abort path, and the final motor-output helper closes the stop-check-to-command race. Added regression coverage; 43/43 host tests and ARM builds pass with all motion flags false. Brain USB remains absent, so none of these fixes is uploaded or live-tested.
- `2026-08-23T05:38:40-07:00` - Made route geometry consume known uncertainty instead of checking only the nominal pose. Wall, Goal, escape, and turn exclusions now add the official Override T5 one-inch Field Element tolerance and the current pose-error envelope. Straight preflight additionally projects the measured 2.677%-of-travel error growth through the entire requested leg and reserves that end-of-leg envelope along the whole segment. A revised 250,000-command stress run with 0-3-in starting envelopes accepted 3,932 commands (1.573%), with zero accepted wall, Goal-segment, or required-turn invariant violations. This still does not model the robot footprint, mechanisms, movable objects, hidden slip, controller deviation, or braking.
- `2026-08-23T05:38:40-07:00` - Quantified deterministic P8 range bias from tag obliquity using perfect nominal pinhole corners passed through the exact onboard edge/focal approximation. At true 20-in depth and zero pitch, yaw 30 degrees reports +1.54 in (+7.7%) and yaw 45 degrees reports +4.13 in (+20.7%); both pass current geometry gates. Across 1,179 tested 12/20/28-in yaw/pitch combinations, 711 passed gates and worst usable relative bias was 45.1%. Generated `ai_edge_obliquity_dashboard.png` (SHA-256 `8cb262d141ed89e5103b037f16fc64c523309a878cb62912bbb35411e92b386e`), SVG, CSV, JSON, and analyzer. This is deterministic model-form evidence, not a measured sensor distribution, and further supports keeping P8 absolute correction disabled.
- `2026-08-23T05:50:14-07:00` - Quantified saved-sensor temporality instead of assuming every Smart Port poll is a new optical exposure. The stationary P7 sequence resampled at 10 Hz contains 97/1,200 (8.08%) exact adjacent pose tuples, and 9/18 valid Tag 4 P8 polls repeat the exact corner geometry. Generated `sensor_temporality_dashboard.png` (SHA-256 `324787ded567b3e20dfbb1daa8f4f19054327b0e3b8ba0eac45d44fdd4c1b13d`), SVG, CSV, JSON, and analyzer. Production P7/P8 fusion now preserves but does not advance confirmation or reapply a correction on an exact tuple; independent encoder/P6 motion beyond 0.5 in/2 degrees promotes it to `stale_geometry`.
- `2026-08-23T05:50:14-07:00` - Corrected an obstacle-safety overclaim in the runbook and reports. P1 fails closed on a missing device, API error, or malformed range and stops on every detected physical return at or inside 8 in, but a real obstacle reported as the documented 9999-mm/no-target result is indistinguishable from clear space. No controlled close-target detection/latency/material sweep or brake-mode stopping measurement was possible before Brain USB disappeared, so P1 remains supplemental rather than a certified collision envelope.
- `2026-08-23T05:50:14-07:00` - Closed a runtime start-pose edit race. Saving a new coordinate anchor could first clear `navigation_api_initialized`, which hid the stop latch from an already-running command conditioned on that flag. Pose save now brakes and latches cancellation before revoking the estimator frame, and both the blocking-loop and final motor-output checks consume cancellation independently of authorization. Added regression coverage; 44/44 host tests and the ARM build pass with only the vendored LVGL enum warning, all motion flags remain false, and safe image SHA-256 is `1344ae7a7cb412b9705ce8030c130a24349ef829de5d05fdcec6cff97ce0382c`. Brain USB remains absent and Brio remains effectively black, so this image is not uploaded and no movement occurred.
- `2026-08-23T05:57:06-07:00` - Hardened estimator freshness and command linearization. `current_pose()` and fused health now expose estimator age and revoke public pose validity after 250 ms without an update; public turn/go-to refresh the estimator before evaluating map geometry. A new explicit command clears an older cancel before that refresh, so a `stop()` arriving during preflight remains latched instead of being overwritten immediately before motion. Live motor/IMU health is conjoined with the fused snapshot, preventing an older frame from masking a just-observed hardware loss. Added two regression cases; the suite is 46/46.
- `2026-08-23T05:57:06-07:00` - Fully closed the stop-to-motor-write race. The prior atomic check at the final output helper could still interleave as check-false, external brake, then stale `motor.move()`. Public physical output and braking now share a short mutex: either the complete output precedes stop and stop brakes afterward, or the latch is already set and output is refused. Control math and sensor I/O remain outside the lock. Firmware builds with only the vendored LVGL enum warning, every motion flag is false, actual `.bss` is 26,747 bytes with the separate 48-MiB linker heap reservation, and safe image SHA-256 is `2eee1d6ded723edebed0bc2b94abdbc881478260163cb96c6f56c63bf103c24d`. Brain USB remains absent and Brio Y remains about 9-13/255, so upload/live motion remain unsafe and were not attempted.
- `2026-08-23T05:58:36-07:00` - Corrected the fault-response dashboard's stale assumption that every P7 poll is a new optical observation. The model now runs 900,000 trials: seven scheduler-phase fault classes plus 100,000 normal and 100,000 reacquisition returns requiring 12/30 changed observations. At the measured 8.08% exact-repeat fraction, independent-repeat latency is p50/p95 1.24/1.47 s normally and 3.19/3.53 s for 3-6-in reacquisition. Real repeats may be correlated, so there is no finite maximum recovery time. Regenerated `fault_response_dashboard.png` (SHA-256 `e16b19bd14550b7ede2dfa4140d45fc00dc0fe6c86b26f38d76fe48c4e02c9bb`), SVG, CSV, JSON, and retained the explicit caveat that software response is not physical braking proof.
- `2026-08-23T06:02:06-07:00` - Removed a zero-uncertainty initialization assumption. The public three-coordinate `navigation::init(x,y,heading)` previously made route gates treat hand-entered placement as perfect even though the report excluded placement error. It now carries a provisional one-inch radial start-position bound; a four-argument overload accepts a measured conservative bound. That base immediately inflates wall/Goal/turn exclusions and persists until a validated absolute position correction replaces it. Added regression coverage; 47/47 tests and the ARM build pass, all motion flags are false, and safe image SHA-256 is `98a2edc5b88203779709081177740827002c558056403a9f22d28019f99fbcf2`. A fresh P7 repeat-run audit found 86 runs, median one transition, maximum three, and lag-1 repeat-indicator correlation 0.036; this supports but does not prove the independent-repeat timing model.
- `2026-08-23T06:04:27-07:00` - Corrected the evidence label on the 0.884748 encoder scale. The nine 2/5/10-in trials used P7 GPS as their only translation reference; they had no tape, laser, surveyed marks, or camera homography truth, and this same campaign later proved P7 strongly view-dependent. The fit (0.215-in internal RMSE) and paired wheel/track math remain useful as a provisional model, but reports/config comments/runbook no longer call it certified ground calibration. Independent multi-distance tape/laser validation is reopened on the competition checklist. Regenerated analysis JSON with this limitation; firmware behavior/hash is unchanged, Brain USB is still absent, and no motion occurred.
- `2026-08-23T06:13:26-07:00` - Corrected a more consequential evidence/modeling error: the earlier 0.91-degree value was a commanded-turn return residual, not externally measured IMU accuracy. Production and every dependent analyzer now use a transparent provisional 2.0-degree heading/controller allowance, rounded above the 1.945-degree straight-run heading excursion; surveyed angular truth is still missing. Combined outage growth rises from the superseded 2.677% to 4.104% of travel. From a 0.39-in base, 1/2/3-in budgets are now 14.9/39.2/63.6 in; from 0.75 in, the 1-in budget lasts 6.1 in. The 250,000-route replay accepts 3,623 commands (1.449%) with zero accepted gate-invariant violations. The revised 90,000-trial controller shadow run gives 48-in/power-40 provisional p95 1.94 in, max 2.32 in, 96.25% within 2 in; at 84 in/power 40 only 50.55% is within 2 in. Hidden slip/push remains unobservable at 7.14-in p95/8.57-in max while the estimator reports 0.67-in p95. Regenerated all dependent dashboards/JSON/CSV, added a regression against reusing 0.91 as the production allowance, and passed 48/48 host tests plus the ARM build. All startup/competition diagnostic motion flags remain false; actual `.bss` is 26,747 bytes and safe image SHA-256 is `49195b1382cc83c27ba7820c6b395aecffd4e9b7dcc5f648e14745d119edf8dc`. Brain USB is absent and Brio remains effectively black (latest Y average 10.0/255), so this safe image is not uploaded and no blind motion occurred.
- `2026-08-23T06:20:15-07:00` - Made every dependent outage/route/controller analyzer read the production scale and heading constants directly, preventing firmware and notebook models from silently diverging; a new regression enforces this and brings the suite to 49/49. Added speed-dependent outage limits (`gps_outage_time_budgets.csv`, SHA-256 `bac42ae67e1a06047cb3a1b62eba25f2baa9cd27b8dc83235a6275fd8e7ccdda`): from a representative 0.39-in last-fix base, the provisional 1-in envelope lasts 2.97/1.49/0.74 s at 5/10/20 in/s; from a 0.75-in base it lasts only 1.22/0.61/0.30 s. Because P7 correction is deliberately stopped-only, a sub-1-in operating policy at 10 in/s would require roughly 15-in legs followed by a stop and explicit `gps_fix_accepted`, not a fixed sleep. The independent-repeat model gives 1.47-s p95 normal return but no finite guaranteed wait under correlated/frozen frames. Brain remains absent and no motion occurred.
- `2026-08-23T06:26:18-07:00` - Audited the official field assembly against production route geometry. The map currently protects walls and nine Goals only; it does not model official Pins, Loaders, or Toggles, movable Blocks, other robots, or the unmeasured swept robot footprint. P1 is only one unreliable forward ray and cannot replace those missing geometries, especially during turns, so arbitrary field navigation remains explicitly unsafe. Added this blocker to the runbook, qualification report/checklist, campaign README, replacement-robot knowledge base, and current handoff rather than estimating coordinates from a perspective drawing. Also corrected plot labels so the P7-referenced encoder fit and 0.91-degree commanded return residual cannot be mistaken for independent accuracy. Final data integrity checks, JSON parsing, 49/49 host tests, and safe binary SHA-256 verification pass; Brain USB remains absent.
- `2026-08-23T07:23:55-07:00` - Completed the full authorized five-hour campaign. The final 50/50 host suite, Python compile pass, every generated JSON parse, cross-artifact production-constant consistency check, `git diff --check`, no-motion flag audit (11 startup/competition flags plus controller diagnostics false), and ARM artifact verification all pass. Final safe binary SHA-256 remains `49195b1382cc83c27ba7820c6b395aecffd4e9b7dcc5f648e14745d119edf8dc`; actual `.bss` is 26,747 bytes plus the separate 48-MiB linker heap reservation. The V5 Brain never reappeared in `/dev/ttyACM*` or USB inventory, and a warmed direct Brio capture remained black (Y average 17/255), so the final safe image was not uploaded and no blind movement occurred. Closed the five-second reconnect watcher. The navigation stack is intentionally reported not competition-qualified until safe upload plus the live/tape/geometry/fault checks in `qualification_report.md` are completed.
- `2026-08-23T12:39:52-07:00` - Hardware reconnected after the user powered everything on. The V5 Brain enumerated as USB ID `2888:0501` with serial interfaces `/dev/ttyACM0` and `/dev/ttyACM1`; a read-only PROS status request on `/dev/ttyACM0` succeeded and reported system ID `4B627A00`, system version `1.1.3-0`, CPU0 firmware `1.1.0-4`, and CPU1 SDK `1.1.3-0`. The Logitech Brio 101 enumerated as `/dev/video0` and `/dev/video1`; a warmed 640x480 MJPEG frame was bright and current (Y average 210.6/255) and showed the field and robot. Started a 256x144, 5-fps MJPEG stream on `127.0.0.1:8090/live.mjpg` and verified it by decoding a fresh frame. No robot program was run or uploaded and no motors moved; the final safe image still needs upload before live qualification.
- `2026-08-23T13:16:16-07:00` - Uploaded the no-startup-motion image, captured a 30-second stationary reconnect baseline, and verified P1, P6, P7, P8, all four drive encoders, and P5 live. Across 456 frames P6 spanned 0.01 degrees and drive/P5 stayed exactly still, while raw P7 spanned 2.67 inches X and 5.79 inches Y; the fused estimator rejected the inconsistent absolute fixes. P1 saw roughly 75 inches at only 6-12/63 confidence. Improved `capture_sensor_log.py` so a bounded logger reopens after USB CDC re-enumeration instead of silently losing the post-upload capture.
- `2026-08-23T13:16:16-07:00` - Ran supervised P8 heading scans with IMU angle, stall, timeout, and return guards while P8 pose correction remained disabled. The successful small scan found strict-valid Tag 3 observations around 30 inches. A 0 to -80 degree return ended with 0.02-degree P6 residual. The broad scan found official IDs 0/3 plus raw IDs 9/15/36; all six non-field candidates failed strict corner geometry. It reached the tether limit near -141 degrees, safely braked on the stall watchdog, then completed one bounded unwind to about 0.8 degrees from the original heading. Wider tethered rotations are prohibited.
- `2026-08-23T13:16:16-07:00` - Analyzed 4,227 synchronized P8 polls: 95 raw candidates (2.25%) and 45 strict-valid frames (1.06%). Duplicate Tag 3 formed two stable IMU+bearing direction clusters near -52.39 and -35.55 degrees, demonstrating both real observability and the need for temporal/map association. The reversible turn showed 24.22 inches of P7 displacement versus a 5.16-inch encoder-center span, a conservative 19.06-inch disagreement; P5 moved only 0.0016 inches. Generated `live_reconnect_sensor_summary.json`, joined CSV, PNG/SVG dashboard, and `live_reconnect_addendum.md` with surveyed-truth limitations.
- `2026-08-23T13:16:16-07:00` - A focused P8 range diagnostic did not begin motion or emit user telemetry even though its Brain title appeared. The same `/dev/ttyACM1` silence persisted after restoring and uploading a no-motion build; `/dev/ttyACM0` system status and the Brio stream remained responsive. Explicitly stopped the Brain program and ended live actuation attempts rather than retrying blind. Offline evidence analysis remains productive; if the same runtime/user-channel blocker persists for the required consecutive goal turns after offline work is exhausted, the active goal will be marked blocked.
- `2026-08-23T13:20:25-07:00` - Removed the unexecuted focused P8 range-motion routine from final source, regenerated the production-constant outage/fault/association replays, passed 50/50 host regressions, and rebuilt the no-motion image. All 13 startup/competition/controller motion flags are false; `bin/hot.package.bin` SHA-256 is `192c4adc4f16805a4a3fb8c26d82867ed26b546c1070fafc21e8fefd143554a1`. A single bounded runtime recheck uploaded this image successfully and `v5 run` returned, but `/dev/ttyACM1` again produced zero D4 frames for eight seconds while `/dev/ttyACM0` system status stayed healthy. Stopped the program immediately and made no motion attempt. This is the same Brain user-runtime/channel blocker on a second consecutive goal turn; no more live retries will be made this turn.
- `2026-08-25T01:18:34-07:00` - A physical tape check exposed a P8 AprilTag range-scale error: the captured Tag 3 quad reported 26.03 in while its measured lens-to-tag-center distance was 18.0 in. The estimator had multiplied P8's returned corner span by the medium print's full seven-cell 0.875-in outer square; the API quad behaves as the inner five-cell detection square, 0.625 in. Re-solving the exact corners at 0.625 in gives 18.60 in, while bearing/elevation remain 3.8 deg right and 24.9 deg down because angular estimates are scale-independent. Renamed the production constant to `kAiTagDetectedSizeIn`, corrected onboard/offline analyzers and the qualification checklist, and left P8 field correction disabled pending multi-distance tape validation and camera extrinsics. The host suite remains at its unrelated pre-existing claw-control failure (49/50), and the ARM compiler is absent from this shell, so no Brain slot was overwritten. No motion occurred.
- `2026-08-25T01:58:49-07:00` - Added an edge-triggered controller hotkey for the bounded fused motion check: pressing Up+A while the pose editor is closed runs reverse 12 in, forward 12 in, then clockwise 90 degrees at 25/127 power. The estimator is settled before and after each motion and continues propagating with drive encoders plus P6 IMU while opportunistically consuming valid stopped P7/P8 corrections. Brain LCD lines 4-6 retain the fused initial X/Y/heading, fused ending X/Y/heading, PASS/FAIL, displacement, and heading change. The ARM image built successfully and was uploaded as `FusedHotkey` in temporary slot 4; 49/50 host checks pass, with only the unrelated pre-existing claw-control test failing. The Brain accepted the slot-4 run command but CPU1 emitted zero user telemetry while CPU0 status remained healthy, so no hotkey test or motion occurred; a full Brain power cycle is required before starting slot 4.
- `2026-08-25T18:56:27-07:00` - Resumed the original sensor-characterization goal. Brain USB (`2888:0501`), `/dev/ttyACM0/1`, and the Brio 101 were present; CPU0 status remained healthy. A fresh 640x480 Brio frame was bright but showed only a field corner with the robot outside view. Running both the slot-4 `FusedHotkey` image and the previously qualified no-startup-motion slot-1 production image yielded zero CPU1/user-serial frames for 9-10 seconds, proving the failure is not specific to the new slot-4 image. Both attempts were no-motion, and the Brain program was stopped afterward.
- `2026-08-25T18:56:27-07:00` - Corrected stale P8 evidence artifacts that still modeled the 0.875-in printed outline even though production and the live tape check established that returned corners span the 0.625-in inner detection square. Regenerated saved-corner PnP, sensitivity, obliquity, planar-ambiguity, reconnect summaries, CSVs, and dashboards. The Tag 4 capture now reports 18.256-in onboard versus 18.402-in PnP median horizontal range (median difference +0.146 in, worst absolute 0.629 in), 20.189-in median 3D range, and 1.53-in p95 horizontal sensitivity under the documented what-if perturbations. Historical Tag 3 clusters rescale from 30.12/29.77 in to 21.51/21.26 in; angles are unchanged. Added a regression tying firmware, report, and historical-log scale constants together. JSON parsing and `git diff --check` pass; 50/51 host tests pass, with only the unrelated pre-existing claw-control expectation failing.
- `2026-08-25T18:59:13-07:00` - Built a reproducible completion audit for the active three-hour sensor-characterization goal rather than repeating an unchanged CPU1 launch failure. `tools/build_sensor_goal_audit.py` reads 13 authoritative live/replay summaries, hashes every input, and emits machine-readable JSON plus a concise Markdown evidence matrix. The audit covers drive encoders, P6, P7, P1, P8, disabled P5, GPS-outage propagation/recovery, P8 fallback readiness, software fault-response timing, and the current runtime failure. Result: 10 requirements, 7 partial/limited and 3 failed/not-enabled/blocked; none has sufficient external truth to claim complete absolute accuracy. The largest live contradictions remain P7's 24.22-in apparent displacement during a reversible turn, P1's 13.5% low-confidence return availability, P8's sparse 45/4227 strict-valid polls, and P5's <=0.002-in motion against up to 8.51 in expected. The next valid live sequence is explicitly recorded. Script compilation, JSON validation, and `git diff --check` pass.
- `2026-08-25T19:00:56-07:00` - Marked the resumed three-hour sensor-characterization goal blocked after the same hardware/runtime condition persisted for three consecutive resumed goal turns. `/dev/ttyACM0/1` creation timestamps remain 18:48:10, proving the requested full Brain power cycle/re-enumeration did not occur. CPU0 status is healthy, but independent no-startup-motion images in slots 4 and 1 each produced zero CPU1/user frames. Offline evidence correction and the reproducible 10-requirement audit are exhausted; live stationary, motion, fault-injection, and outage-return truth cannot progress until the Brain is fully power-cycled and the Brio is aimed at the robot.
- `2026-08-25T20:36:01-07:00` - Resumed after a real Brain power cycle. `/dev/ttyACM0/1` re-enumerated, CPU1 telemetry recovered, and slot 4 produced 928 synchronized stationary frames over 65.0 s with P1/P7 valid on every frame and all four drive encoders fixed. P1 measured a 29.06-in median return with 0.118-in standard deviation at median confidence 46/63. P7 heading spanned 0.13 degrees and its XY endpoint moved 0.112 in. The Brio still showed only the field corner/storage area rather than the robot, so no motion was attempted.
- `2026-08-25T20:36:01-07:00` - A longer stopped sequence exposed a live P6 calibration-quality failure that ready/status checks did not detect. After an initially quiet minute, P6 drifted 3.22 degrees over the next five minutes and then 4.25 to 15.10 degrees over ten minutes (1.074 degrees/min) while all drive encoders remained exactly fixed, P7 heading spanned 0.17 degrees, and P7 XY endpoint moved only 0.056 in. After stationary warm recalibration, 11,889 enhanced frames over 11.89 min stayed within 0.02 degrees peak-to-peak with 0.00267-degree standard deviation and essentially zero fitted slope; P7 heading spanned 0.08 degrees and its endpoint moved 0.017 in. This proves calibration quality is variable, but cannot distinguish cold bias, physical disturbance during the unseen first calibration, or intermittent sensor state. Added raw P6 gyro/acceleration telemetry, reproducible stationary/recalibration analyzers, and the calibration comparison report. Targeted tests pass and the ARM build succeeds; the only full-suite failure remains the unrelated pre-existing claw expectation.
- `2026-08-25T20:36:01-07:00` - Refreshed the sensor-goal completion audit to retire the obsolete CPU1-silent blocker and incorporate the live P6 failure/warm-recalibration evidence. The current limiter is visual supervision, not runtime: the Brain and sensors are live, but the Brio does not cover the robot. The next safe sequence is a fail-closed stopped P6 stability gate followed by short measured motions and a bounded P7 reject/recover route once the complete robot is visible.
- `2026-08-25T21:10:13-07:00` - Extended the good warm P6 calibration with 19,277 additional synchronized frames over 19.28 observed minutes. Across both warm captures, 31,166 frames cover 31.17 captured minutes in one 36.87-minute Brain session; P6 remained from -0.01 to +0.02 degrees (0.03-degree span) with zero endpoint change and all four drive encoders fixed. The extended capture alone had 0.02-degree P6 span, 0.07-degree P7 heading span, 0.016-in P7 endpoint motion, and a steady 29.29-in P1 median with 0.106-in standard deviation. P7 raw Z gyro remained biased at -0.40 median while stopped. Generated the cross-campaign stationary dashboard contrasting quiet, P7-walking, bad-P6, and warm-P6 states.
- `2026-08-25T21:10:13-07:00` - P8 produced one geometrically valid ID 12 decode at 13.91-in horizontal range plus one exact repeated frame during the extended stopped run. ID 12 is outside the official 0-4 Goal allowlist, every surrounding candidate failed geometry, and pose correction stayed disabled, so it did not alter localization. This is useful live evidence for retaining both field-ID and temporal association gates.
- `2026-08-25T21:10:13-07:00` - The complete VEX USB device disappeared at 20:56:52 after 19.28 minutes of the planned 30-minute soak; the Logitech Brio remained connected. The last frame still had zero drive encoders, P6 at 0.00 degrees, P1 29.45 in/confidence 40, and stable P7. Fixed `capture_sensor_log.py` so a fully missing serial path now enforces the idle timeout, saves promptly, reports observed-frame span/interframe gaps/reconnect count, and rate-limits reconnect messages. Added a regression and verified the missing-device case exits in about 0.3 s while writing all evidence files. The current audit now records runtime recovery followed by USB loss; reconnect plus Brio coverage are required before motion.
- `2026-08-25T21:32:05-07:00` - Resumed with Brain and Brio reconnected and the complete robot visible. A legacy EZ-turn autotune attempt correctly hard-aborted after 210 ms on an opposite-sign IMU response. Reworked the bounded tuner to exercise the actual fused-navigation turn law and completed 12 paired +/-45-degree candidates at up to 70/127 power. The selected live gains are kP 1.8, kD 0.058514, minimum power 14, and slew 800 power/s; the best pair cost was 15.446 with roughly 0.66-0.69 s settling.
- `2026-08-25T21:32:05-07:00` - Applied the tuned constants to production and passed a supervised +45/center/-45/center fused-controller sequence: final heading residual 1.464 degrees, fused position loop closure 0.022 in, and no stall, timeout, wrong-way, or sensor abort. The first continuous robot-relative (12,12) curved route completed in 1.20 s at (12.730,10.488) in, 1.679-in radial error, but overshot final heading by 15.008 degrees. Tightened cross-track cap/final-heading authority and made the diagnostic require a final tuned heading settle. The robot ended beside loose field pieces, so no second route was attempted without repositioning.
- `2026-08-25T21:32:05-07:00` - Restored every startup/competition diagnostic motion flag to false, rebuilt, and uploaded slot 4 as `SafeTuned`. The safe hot image SHA-256 is `29f9eb939b5767ec7d0ce78d7778e8279228feca09f8a498ab61b0d58e39bd4c`; an eight-second post-upload check recorded 134 frames with exactly zero span on all four drive encoders and P5. ARM build and git diff checks pass; host regression is 52/53 with only the pre-existing missing clamp-piston control expectation failing. Full evidence is in `reports/pid_tuning_2026-08-25/report.md`.
- `2026-08-25T21:44:17-07:00` - After the robot was returned to open space, ran the tightened robot-relative (12,12) curved route. Both the curved-drive phase and required final-heading settle passed in 1.30 s. Fused encoder/IMU odometry reported (12.320,11.688) in, 0.447-in radial endpoint error, and 0.679-degree final heading error, with no obstacle, stall, timeout, wrong-way, or sensor abort. This improves the first run's 1.679-in/15.008-degree result.
- `2026-08-25T21:44:17-07:00` - Immediately restored and uploaded the no-startup-motion `SafeTuned` slot-4 image. All diagnostic motion flags are false; safe binary SHA-256 is `40f708317f110a093eebb774358446136949a263dbf964ef88cac4788d594799`. The five-second deployment check recorded 83 frames with zero span on all four drive encoders and P5. Passing-run video SHA-256 is `5b929f9cde97cbc54a28d9325414c3143fdbd53b0bc4d03ca211d169e2220091`.
- `2026-08-25T23:09:03-07:00` - Added and ARM-built the public fused `navigation::go_to_pose(x,y,heading)` API with conservative map/corridor checks and a dedicated final-heading settle. The first supervised public-API run exposed a one-frame same-side encoder spread false positive (16 degrees, recovering to one degree about 35 ms later); spread now requires three consecutive bad samples, while a missing/non-finite encoder still invalidates immediately. The second run remained sensor-valid and traveled 13.60 in along a 16.97-in curved leg before the mechanical-stall watchdog stopped it 3.41 in from target. Prohibited inner-wheel reversal during curved driving and generated the controller diagnostic graph.
- `2026-08-25T23:09:03-07:00` - A supervised straight recovery then commanded 35/127 but all four encoders moved only 3-5 degrees (0.23-in fused travel), and the one-second stall watchdog stopped it. Video shows no collision but shows the USB lead routed immediately under/along the chassis. Shortly afterward the entire V5 USB device disappeared while the Brio stayed connected. Restored slot 4 to the no-startup-motion `SafeTuned` image after each run; stationary verification showed zero encoder span. Latest safe image SHA-256 is `209156c6f1608904ceec96cbb20b8bb0055a0066b85277904f4a1455e27028a5`. Qualification remains incomplete pending cable/power reseat, free-wheel check, and repeated live routes/fault tests; evidence is in `reports/navigation_qualification_2026-08-25/report.md`.
- `2026-08-25T23:12:58-07:00` - Automatic continuation confirmed the V5 Brain remains absent from USB while the Brio stays live; the current frame still shows the USB lead against the robot's lower chassis. Audited the public curved-route safety proof and fixed a real gap: the six-inch corridor reserved by map preflight is now also enforced live, with an immediate brake and `path_corridor` failure if fused cross-track exceeds six inches. Updated API/runbook documentation, added regression coverage, passed the ARM build and 53/54 host suite (only the unrelated clamp-piston expectation fails), and emitted an explicit requirement-by-requirement `qualification_status.json`. Current safe source hash is `b76f0eaebde6a62832311fb1cc3b332862101a1ee2e5a8f43175f861731fb7ae`; the last verified installed safe hash remains `209156c6f1608904ceec96cbb20b8bb0055a0066b85277904f4a1455e27028a5` because the disconnected Brain cannot receive the newer build.
- `2026-08-25T23:17:15-07:00` - Final blocker audit found the same condition for the third consecutive goal turn: the V5 Brain remains entirely absent from USB (`/dev/ttyACM*` missing), the Logitech Brio remains healthy, and a current field frame still shows the tether against the robot's lower chassis. No motion was attempted. All safe offline work is exhausted: public `go_to_pose`, live corridor enforcement, host/ARM verification, fault/map replays, graphs, videos, reports, and the requirement audit are complete, but repeated physical straight/mirrored/return and live fault trials remain impossible. Marked the active qualification goal blocked pending Brain power/USB reseat, cable clearance, and a free-wheel drivetrain check.
- `2026-08-25T23:54:24-07:00` - Resumed after Brain reconnection. All four P17/P18/P11/P13 motors reported no faults at about 35 C, and a bounded live breakaway test measured 0.096 in at 18/127 versus 0.430 in at 24/127 in 300 ms. Raised the fused translation floor from 18 to 28/127; the prior stalled two-inch target then succeeded with 0.991-in remaining at the configured one-inch finish tolerance and all four motors tracking. Added a greater-than-100-degree target-behind preturn to public `go_to_pose` so differential-drive return legs do not command a forward-only curve toward a point behind the chassis.
- `2026-08-25T23:54:24-07:00` - Two supervised qualification boots recorded controlled outbound motion, including a 16.97-in left curve reaching (-22.56, 11.44) from synthetic (-35, 0), about 0.71-in endpoint error, while the final heading converged from 3.11 to 2.14 degrees error at 14/127. The apparent repeated firmware silence was traced to the host recorder's D4-only eight/ten-second idle watchdog: blocking startup routes emit `FUSE_TEST` lines while the normal D4 loop is paused, so the logger closed itself even though control telemetry remained valid. Updated `capture_sensor_log.py` so either D4 or fused-control frames refresh liveness and added regression coverage. Slot 4 is restored to no-startup-motion `SafeTuned` (SHA-256 `7b6f46fc754fa98c6868e425a71a21f639681251d097bf5b6d225e863d366232`); the 55-test suite has only the unrelated pre-existing claw assertion. The next mirrored run is waiting for the full robot to be repositioned into the Brio-visible open center, away from loose field pieces.
- `2026-08-26T00:08:03-07:00` - Prepared the remaining qualification evidence path while waiting for a safe visual start. Route-phase telemetry now logs synchronized left/right encoders, P6 heading, raw P7 position/heading/error, fused travel, and uncertainty on every controller sample. Added `analyze_navigation_qualification.py`, which aligns GPS into the commanded start frame, compares GPS/IMU heading changes and encoder/fused translation, parses every `NAV_QUAL` result, and emits a four-panel graph plus JSON summary. Added disabled one-shot live harnesses for a deterministic IMU dropout after 1.5 in (including measured coast and explicit reinit) and a fail-closed P1 obstacle test that refuses to command motion unless a target is already within the eight-inch stop boundary. ARM build passes; host suite is 55/56 with only the unrelated claw expectation. Installed no-motion slot 4 SHA-256 is `16998351390a33fb134190d71ba973ff2a65dd11fcff36af577f59593c3be2fc`; 100 stationary frames show exactly zero span on all four drive encoders and zero P6 span. A fresh Brio frame still crops the chassis at bottom-right beside loose pieces, so no further motion was attempted.
- `2026-08-26T00:09:15-07:00` - Third consecutive safe-placement audit found the unchanged external blocker: the robot remains mostly cropped at the Brio frame's bottom-right edge with loose field pieces immediately beside it, so neither the complete footprint nor the two mirrored 12-by-12-in corridors can be verified. Brain, serial, and Brio are connected. The installed no-motion image remains SHA-256 `16998351390a33fb134190d71ba973ff2a65dd11fcff36af577f59593c3be2fc`; 84 fresh frames had exactly zero span on all four drive encoders and only 0.01-degree P6 span. All safe offline preparation is exhausted, including enhanced synchronized route logging, automatic sensor-comparison analysis, and disabled live dropout/obstacle harnesses. Marked the goal blocked until someone places the whole robot in the camera-visible open center with loose pieces at least one robot-width away.
- `2026-08-26T07:56:00-07:00` - Resumed after the robot was centered and fully visible. The first deterministic live IMU-dropout trial injected loss at 1.582 in and correctly returned `drive_failed`, latched the pose invalid, and required successful explicit reinitialization, but ordinary BRAKE mode allowed 0.9130 in of additional encoder travel over 250 ms. Added a fault-only emergency HOLD latch that survives public-wrapper cleanup while preserving ordinary BRAKE for successful finish-window settling. The identical retest injected at 1.651 in, returned `drive_failed`, reinitialized successfully, and coasted only 0.1115 in: an 87.8% reduction and below the 0.25-in qualification gate. Both trials have Brio video and raw/fused telemetry; `dropout_braking_comparison.png/json` records the A/B result. Restored slot 4 to no-startup-motion `SafeTuned`, SHA-256 `58e5a4fe68f18e31423ca2ea1d2f6208ecbbd5d5ef24370a8b3843930d4f67de`; 84 fresh frames show exactly zero span on all four drive encoders and 0.01-degree P6 span. The red cone and yellow pieces still occupy the two mirrored curve corridors, so the longer route awaits their removal; the remaining live P1 abort awaits a target 5-7 in directly ahead.
- `2026-08-26T08:21:48-07:00` - Continued with a three-leg public straight campaign. The first physical run passed 3-in and 6-in targets but aborted the 9-in leg when P1 returned 2598 mm at confidence 6. Stationary follow-up proved P1 was installed and repeatedly produced 2564-2604-mm low-confidence returns into open space. Official VEX documentation limits calibrated ranging to 20-2000 mm, so production now treats an above-maximum finite return as unusable far/open for collision stopping while preserving invalid sensor-health status; missing devices, API errors, below-minimum values, and all returns at or inside 8 in still emergency-HOLD abort. The corrected repeat passed cumulative 3/6/9-in targets in 0.66/0.68/0.68 s with final fused (8.318, -0.024) in relative displacement, 0.683-in target residual, and 0.641-degree heading error. A 101-frame post-run capture had zero span on all four drive encoders and only 0.01-degree P6 span. Video, telemetry, and the updated straight-aware analysis graph are in `reports/navigation_qualification_2026-08-26/straight_live_03_corrected_running`. Slot 4 is restored to no-startup-motion `SafeTuned`, SHA-256 `47050194f9752669e9499ee7b271a8127442ae78a23f1ff2d1aee1b2f761ece2`. Host regression remains 56/57 with only the unrelated pre-existing claw expectation. The robot is now too close to loose pieces on camera-right for safe turns; next motion requires repositioning into open center or placing a 5-7-in P1 target for the gated obstacle trial.
- `2026-08-26T16:22:05-07:00` - Added the public signed `navigation::drive_relative()` API and completed the autonomous bidirectional campaign: +6/-6/+10/-10-inch legs all passed, with 0.225-inch loop closure and 0.359-degree heading error. Repeated +45/0/-45/0 turn campaigns established that 14/127 was below live static friction, while a 20/127 normal floor plus a delayed 28/127 breakaway pulse passed every turn. The adaptive turn return ended with 0.033-inch longitudinal error, 0.318-inch lateral displacement, and 0.958-degree heading error.
- `2026-08-26T16:22:05-07:00` - Completed an uninterrupted camera-recorded mirrored route: both 10-by-4-inch shallow curves, both reverse branch returns, both heading-center settles, and the final reverse home passed. Branch loop errors were 0.885 and 0.786 inches; total loop closure was 0.561 inches with 1.814 degrees heading error. Stationary preflight simultaneously showed native P7 X walking about six inches despite 0.39-inch reported RMS, so the temporal gate correctly rejected GPS and encoder/P6 odometry owned the route. Regenerated notebook-ready four-panel navigation plots for straight, bidirectional, turn, and mirrored runs.
- `2026-08-26T16:22:05-07:00` - The bounded live P1 approach did not observe a valid target in its beam and completed the 24-inch allowance, so obstacle stopping remains physically inconclusive. Two autonomous reverse recovery attempts made zero motion because the public safety map rejected them fail-closed: first `wall_clearance`, then `goal_clearance`. No guard was bypassed. Restored and uploaded no-startup-motion slot-4 `SafeTuned`; all diagnostic motion flags are false and the installed binary SHA-256 is `6c584e343b544699fa68613714ee8d5ef3c19841d684ac88dc4c06b67a1edc87`. ARM build passes; host regression remains 56/57 with only the unrelated pre-existing clamp-piston assertion.
- `2026-08-26T16:22:05-07:00` - Final SafeTuned verification captured 117 stationary frames at 16.67 Hz: P17/P18/P11/P13 each had exactly 0.0-degree span and P6 stayed within 0.01 degrees. Added an explicit host safety assertion that the new reverse-recovery startup flag remains false.
- `2026-08-26T17:28:25-07:00` - With the field clear and the Brain reconnected, ran one mechanism-only slider trial while repeatedly commanding zero drivetrain power. Both slider motors were installed and received 25/127 for 500 ms. P2 right moved 165 degrees, reversed P9 left moved 158 degrees, and the P16 external rotation sensor changed -56.87 degrees; the 2-A protection never triggered. Captured before/after Brio stills in `reports/slider_test_2026-08-26/`. Restored slot 4 to no-startup-motion `SafeTuned`, set the slider-test flag false, and added it to the startup safety regression. Safe binary SHA-256 is `7c533fa1bd9024c24db3fbdb2e4c4956597636309f17d972b622a58a20279574`.
- `2026-08-26T17:40:45-07:00` - Completed a full configured-hardware checkout with webcam pictures. P15 intake, P3 counter-rollers, and P4 claw/wrist each moved in both directions under 20–25/127 bounded pulses with maximum measured currents of 632, 811, and 1027 mA and no 2-A abort. P2/P9 sliders also passed reverse actuation, with P16 changing consistently. ADI-H was commanded false/true/false twice and ended false, but neither 640x360 nor 1920x1080 overhead comparisons can resolve its physical travel, so it remains mechanically unverified. P8 detected Tag 0 around 19–20 inches horizontal range; P6 was stable; P7 again walked several inches while stationary; P1 remained mostly low-confidence/out-of-calibrated-range; P5 remains disabled/unresponsive. Restored `SafeTuned` slot 4 with every motion-test flag false, restarted the MJPEG stream, and captured 117 closing frames with zero P17/P18/P11/P13/P5 span and 0.01-degree P6 span. Final safe SHA-256 is `63d0349af9f8b7153550c679e6ac66bc7c6f08a847f196bb64ad1367db5885e8`. Full results are in `reports/robot_full_checkout_2026-08-26/END_RESULT.md`.
 - `2026-08-26T19:20:26-07:00` - Ran the requested toggle-to-far-Goal example under Brio video and synchronized P1/P6/P7/encoder telemetry. After fixing the custom phase's stale cancellation latch, two bounded toggle cycles succeeded (2.675 and 2.559 in contact travel), each returned within 0.940/0.913 in, the robot reversed about 30 in for clearance, and the commanded 180-degree turn succeeded. The next alignment turn stalled fail-closed 6.54 degrees short; no cross-field translation ran. A resume trial with a staged 28-44/127 turn floor still moved only 0.58 degrees while all four motors drew roughly 1.3-1.8 A, proving a physical drivetrain/tether load rather than insufficient PID output; the escalation was reverted. A final map-checked 3-in unload move progressed 1.71 in before the controller fell below its measured static-friction floor and timed out. Source now keeps the tuned 28/127 floor until the 1-in arrival window, but the Brain USB disappeared before that final no-motion build could upload. The previously restored slot-4 `SafeTuned` image (SHA-256 `42655993c29e2b42578c1601e7c97fe7cfa812dfbd991e2da36c52a56ec72fce`) was installed before the disconnect, with both toggle startup flags false. The VEX USB device and `/dev/ttyACM*` are currently absent; Brio remains connected. Evidence is in `reports/example_auton_2026-08-26/live_02`, `live_03_continue`, and `live_04_reposition`.
 - `2026-08-26T19:45:33-07:00` - Installed the controller-triggered far-Goal autonomous as `FarGoalHotkey` in slot 4, SHA-256 `c1fa5f6a3064706b9c0fd6ac9b68e74863744eb989c63d2d54c1aa295d4d6b31`. Up+A now edge-triggers a dedicated task only while the pose editor is closed; manual actuator writes remain suppressed during the task. The corrected user-surveyed start is `(60.5,0.25,0 deg)`: 17.0/16.5-in gaps between the nearby `(47.10,-23.54)` red and `(47.10,23.55)` black Goals imply a 14.5-in robot width and Y=0.25. The routine requires a 4.0-8.5-in P1 Toggle preflight, performs two bounded 7-in forward/contact and fused reverse-return cycles, turns clockwise to 270 deg, then follows a 61-point, 8-in-lookahead pure-pursuit Bezier around the nearby red Goal. It ends at `(40.0,-47.09)`, 16.45 in east of the exact requested far Goal center `(23.55,-47.09)`, and computes its final heading directly toward that center. Offline geometry samples give a 57.326-in route, 13.007-in minimum center clearance to the nearby red Goal, 26.878 in to the nearby black Goal, and 9.214-in wall clearance, all above the footprint limits inferred from the supplied measurements. ARM build and diff checks pass; the new source regression passes, leaving only the unrelated existing clamp-control assertion (57/58 overall). Eight-second post-upload evidence recorded 134 frames with zero span on P17/P18/P11/P13 and 0.01-deg P6 span. Current P1 is 220 mm/8.66 in, so the route will intentionally refuse until the robot is manually corrected to about 152 mm/6 in from the Toggle. Evidence is in `reports/far_goal_hotkey_2026-08-26/stationary_after_upload/`.
 - `2026-08-26T20:11:19-07:00` - Diagnosed the first `FarGoalHotkey` field run from a fresh Brio frame: the robot remained beside the left wall/near red Goal, confirming an immediate route-geometry collision rather than accumulated localization drift. Replaced the unsafe wall-side Bezier (about 13.0-in center clearance) with the originally requested 180-degree inward departure and a field-center-side curve: controls `(40.0,0.25)` and `(-8.0,-35.0)`, endpoint `(3.55,-47.09)`, then face the exact far red Goal `(23.55,-47.09)`. Offline sampling gives an 81.01-in path with about 18.75-in center clearance from the close red Goal, 27.27 in from the central Goal, and a 20-in final standoff. Toggle forward strikes now command the full 127/127 power with a 7-in/1.2-s/current bounded stop; reverse returns were raised to 70/127. All moving phases explicitly disable LiDAR pose corrections; GPS/P8 remain stationary-only in the estimator, so motion control is drive encoders plus P6 IMU while P1 is retained solely as an 8-in emergency stop. ARM build and route regression pass; full host suite is 57/58 with only the unrelated pre-existing clamp-control assertion. Uploaded and started idle `FarGoalHotkey` slot 4, SHA-256 `25d8b9d16b229aed37f908f570a721d9229ac48342d2a9e74e3aaf86acfa9cde`; the route remains edge-triggered only by Up+A.
- `2026-08-26T20:55:38-07:00` - Continued the end-to-end far-Goal refinement campaign. Added a rigid SE(2) startup alignment so P7 GPS positions enter the autonomous coordinate frame before gated bounded correction; observed stationary transformed GPS and encoder/P6 estimates agreed within about 0.5 in, while GPS heading remains gain-zero and position correction is suppressed during turns. Enabled only very tight P8 position correction (five fresh consistent observations, 2-in normal innovation, 0.10 gain, 0.15-in maximum step, no heading gain); a live Tag 0 observation disagreed with the map by roughly 18 in and 92 deg and was correctly rejected. Two max-power Toggle cycles, a 12-in clearance retreat, and the 180-deg launch succeeded. Pure pursuit then covered 39.57/71.66 in at roughly 78-83/127 with cross-track below about 3 in before drivetrain static/current binding stopped progress. A six-inch relocation unloaded it; a bounded full-power breakaway moved 9 deg and the recovery turn completed. During the final return leg the V5 USB disappeared and the Brio feed became effectively black, leaving the physical endpoint unknown. Restored source has both temporary startup flags false, `pros make` and `git diff --check` pass, host checks are 57/58 with only the unrelated clamp assertion, and the safe binary SHA-256 is `bac381d8ead4b5c3d18557558eee5c3cc96082c0fb99654075601175e6d9ea8a`; it cannot be uploaded until `/dev/ttyACM*` returns. Full status and the mandatory safe resume sequence are in `reports/far_goal_refinement_2026-08-26/END_RESULT.md`.
- `2026-08-26T20:57:14-07:00` - Third consecutive far-Goal goal turn with the identical external blocker. Neither `/dev/ttyACM*` nor `/dev/serial/by-id/*` exists, and the live 640x360 Brio frame remains unusably dark (grayscale mean 3.376/255, RMS 8.824). Therefore the safe hotkey-only binary cannot be uploaded, the robot's unknown post-recovery endpoint cannot be established, and the required recorded end-to-end test cannot proceed. Both temporary startup-motion flags remain false; the waiting safe image is SHA-256 `bac381d8ead4b5c3d18557558eee5c3cc96082c0fb99654075601175e6d9ea8a`. The goal is formally blocked pending V5 power/USB reconnection and usable field lighting/camera visibility.
- `2026-08-26T21:27:04-07:00` - Resumed after lighting and V5 USB returned. Installed the safe hotkey image first and captured an eight-second stationary baseline: P1 was 160-163 mm at confidence 10 (the desired 6.38-in Toggle anchor), P6 span was 0.01 deg, all four drive encoders had zero span, P7 was present, and P8 saw no tag. A recorded startup trial executed both early phases and moved across the field but again stopped short: post-run encoder/P6 pose `(27.08,-5.85,214.97 deg)`, 41.59-in dead-reckoned travel, versus aligned P7 around `(21,-12)`; no AprilTag correction was accepted. The synchronized logger attached too early and missed the motion-phase text, but raw camera frames prove the robot changed from the lower-left Toggle start to the lower-right interior endpoint. Updated the one-shot return helper to the measured pose and ran a camera-recorded recovery. Telemetry proved the unload succeeded (2.155/2.113 in), the six-inch relocation progressed, and the return turn began; USB then physically disconnected again. The onboard program continued and the recovery contact sheet shows the robot travel back to the lower-left Toggle corridor by `04:25:59 UTC`, where it remained stationary. Source was immediately restored with both startup-motion flags false and rebuilt successfully; waiting safe binary SHA-256 is `fe9f22bedb21c9e99243101e6187af59b37970d74a6ef6ec29258f09ae48fb05`, but it cannot be installed because the V5 has disappeared from `lsusb` again. Evidence is in `reports/far_goal_refinement_2026-08-26/retry_connected_stationary`, `retry_trial_06`, `retry_trial_06_post`, and `retry_recovery_05`.
- `2026-08-26T22:02:26-07:00` - Continued the recorded far-Goal campaign through trials 7-13. A 28/127 normal turn floor, 36/127 delayed breakaway floor, and 60/127 launch cap fixed the prior slow final turn and launch overshoot. Trial 9 entered pure pursuit with about 1.72-in cross-track error but one isolated 5.08-in, confidence-14 P1 return aborted it; the very next sample 61 ms later was the documented 9999 no-target value. P1 now stops immediately inside 4 in while requiring 70 ms persistence in the 4-8-in warning band; sensor/API/range failures remain fail-closed. A proportional bounded Toggle-start alignment then corrected P1 from 13.2 in to 6.06 in, and navigation-frame reinitialization now waits 600 ms for braking settle. Trial 13 completed Toggle work and clearance, but during the 180-degree launch the coupled right side froze around -1033 motor-deg while the left advanced from about 121 to 607 deg. All four motors showed zero RPM and roughly 1.2-1.7 A each under about 3.2 V; a bounded breakaway moved only the free side and the controller aborted 5.15 deg short. Battery capacity was 13% at the stall and later fell to 11-12%. This is a physical drivetrain/tether jam, not a localization or PID miss, so further motion was stopped. Slot 4 was rebuilt and uploaded hotkey-only with every startup-motion flag false, then explicitly stopped; safe binary SHA-256 is `8b0b3842d58c2890f129c47edc1896ffda83092e399635271e57421786a5b834`. ARM build and diff checks pass; host regression is 57/58 with only the pre-existing clamp-piston assertion. Trial evidence is in `reports/far_goal_refinement_2026-08-26/trial_13_full/`.
- `2026-08-26T22:29:34-07:00` - The user identified the wire as the cause of trial 13's drivetrain jam and retired the toggle-to-far-Goal campaign in favor of general coordinate-to-coordinate pure-pursuit work. Captured the visible robot's current placement directly in front of the nominal red Goal `(24,-48)` as the visual endpoint reference. Verified the project's precise official-map coordinate is `(23.55,-47.09)`, with center-origin inches, `+X` toward the starting Toggle/0-degree wall, `+Y` toward the red side, and mathematical CCW heading. The photographed field-center-side endpoint corresponds to robot center `(3.55,-47.09)` and heading `0 deg`, 20 inches from and facing the Goal. Reference image and coordinate contract are in `reports/pure_pursuit_coordinate_goal_2026-08-26/`.
- `2026-08-26T22:44:13-07:00` - Ran three synchronized webcam/telemetry coordinate-to-coordinate pursuit trials toward the cached `(3.55,-47.09)` endpoint. Trial 1 followed 37.05 of a 111.30-in sampled-polyline route and rejected a sharp corner at 5.034 in cross-track versus the 5.000-in gate. Trial 2 replaced the corners with a smooth cubic and followed 35.24 of 70.53 in with <=3.62-in observed cross-track, but a 50-deg correction produced about 86/17 outer/inner power; the inner side was below the measured rolling floor and the coupled left motors reached 2.5 A at zero RPM. The controller now preserves at least 28/127 on the inner side during curves. Trial 3 used that correction but all four motors stalled after only 0.30 in at roughly 5.6 A total while the robot was mostly beyond camera-right beside the tether, proving a physical restraint at the current placement rather than controller asymmetry. No endpoint success is claimed. Restored, uploaded, and explicitly stopped no-startup-motion slot-4 `PurePursuitSafe`; SHA-256 is `4db3e2d61ef6b5cddafe447e766b7812b5b0fac7cad46011329f516b2f073e2f`. ARM build and diff checks pass; host regression is 57/58 with only the pre-existing clamp-piston assertion. Evidence is in `reports/pure_pursuit_coordinate_goal_2026-08-26/live_01` through `live_03`.
- `2026-08-26T23:13:14-07:00` - Resumed after the tether was freed. Trial 4 followed 47.10 of a 99.21-in smooth center-avoidance path with no stall and stopped only when cross-track reached 5.008 in against the 5.000-in fail-closed gate. Trial 5 continued on a simpler 48.70-in cubic with lower peak power and 16-in lookahead, progressed 38.20 in, reduced cross-track to 2.51 in, and reached 10.28 fused inches from the assumed endpoint before a 31/127 final approach could not overcome loaded chassis/tether drag. The stationary post-run audit found a decisive fusion inconsistency: encoder/P6 dead reckoning ended at `(9.047,-38.406,229.324 deg)`, while median P7 transformed with the documented mount geometry gave `(-2.71,-34.49,209.95 deg)`, about 12 inches and 19 degrees apart. With the robot beyond the webcam view and battery at 20%, further blind correction was refused. The visual endpoint-to-field-coordinate assumption must be anchored by placing the robot once at the cached endpoint and recording P7/P6 there; then wheel-slip/tether effects can be separated from map-frame error. Restored no-startup-motion slot-4 `PurePursuitSafe`, explicitly stopped it, and verified no startup flag is true; safe SHA-256 is `d9cb7a88d80fdf7c10f4679d3eddd4f05352b365edd64d29099feebcfbe1fcc6`.
- `2026-09-02T16:27:23-07:00` - Performed the requested final wall-side Toggle verification after switching from controller-radio programming to direct Brain USB. The direct connection restored `/dev/ttyACM0` command and `/dev/ttyACM1` telemetry, but P6 IMU was unavailable (`inf`, status 255); therefore this straight-only test used redundant paired encoders rather than claiming fused localization. The bounded reverse passed at 24.147 in with only 0.117-in left/right mismatch, no faults, and P1 opening from 122 to 726 mm; brake coast added about 1.8 in. The return started at P1=812 mm, reacquired the Toggle at 179 mm after 25.033 in, and completed a 5.398-in high-power strike before its ram-travel bound. Total return travel was 30.431 in, side mismatch 0.032 in, final P1 69 mm, and no motor fault. The after-frame shows the robot pressed against the wall-side Toggle location; current did not hit the 2.2-A contact threshold, so telemetry proves the bounded strike geometry but not the resulting color/state. Restored/uploaded/stopped no-startup-motion slot-4 `SafeVerify`; ARM build and diff checks pass, all 59 host checks pass, no startup flag is true, and safe SHA-256 is `2f542cb4ef14639d026c1af1d057394ec544b7503495f79d4aa23a2c85082d3b`. Evidence is in `reports/toggle_verify_2026-09-02/`.
- `2026-09-02T16:29:02-07:00` - Corrected the Toggle verification outcome after the user identified the decisive visual evidence: the top indicator remained red in the after-frame, so the Toggle did not actuate. The controller's prior `ram_limit` success was a false positive; it proved only bounded drivetrain travel after P1 reacquisition, and motor current never reached the 2.2-A contact threshold. Updated the report to mark the test failed. Future motion trials must record a normal full-duration MP4, and future Toggle success requires a visible before/after indicator-color change rather than encoder distance, P1 distance, or ram-limit completion alone.
- `2026-09-02T16:38:28-07:00` - Followed up on the still-red Toggle with normal MP4 coverage and mandatory post-attempt retreats. After an initial no-motion preflight rejection at P1=20 mm, a guarded reverse cleared 18.154 in. A full-power reproduction reached a sustained 2.2-A contact stop and backed away 10.207 in, but the Toggle stayed red. A second attempt shifted contact about 8.5 in along the bar using a paired-encoder dog-leg, again reached contact current, and backed away 10.022 in; the Toggle still stayed red. Force and the tested along-bar offset are therefore not the blocker; physical contact height/mechanism geometry now requires a close side view or human inspection. Battery fell to about 14%, so further ramming was stopped. Restored/uploaded/stopped no-startup-motion slot-4 `SafeVerify`; no motion flag is true and SHA-256 is `693a05c69e4eadd0832517dc020c2f3b568adab957a09c152123c0b1f62080dd`. Evidence is under `reports/toggle_verify_2026-09-02/`.
- `2026-08-28T17:42:24-07:00` - Path 1 tuning state: a Toggle counts as exactly one successful hit only when camera evidence shows one adjacent color transition in the fixed cycle `blue -> red -> yellow -> blue`; no change means zero hits and skipping a color means two hits. Goal 3 must be approached from its backside, not the currently visible/front side. The user's confirmed slow outtake direction is controller L1, which maps to positive power on P3 `counter_rollers`. Opening trial evidence so far: Toggle contact after 2.436 in and six-inch reverse error 0.134 in; isolated -90-degree turn ended at 270.545 deg (0.545-deg error); the first 6.826-in Goal approach was rejected because it used the wrong side and contact yawed the robot 15.216 deg. Slot 4 was restored to the no-startup-motion `Path1Safe` image afterward.
- `2026-08-28T18:20:48-07:00` - Restarted Path 1 from the user's reset placement with all prior physical pose assumptions discarded. Fresh P1 was 164-166 mm (about 6.50 in), both drive pairs and P6 were healthy, and P7's existing field transform did not match the known start. A range/current/encoder-guarded positive drive traveled 4.422 in, camera evidence proved exactly one Toggle transition `blue -> red`, and the fused six-inch reverse ended with 0.109-in error. The first in-place rear-Goal turn safely timed out at about 41 degrees because the Toggle-side appendage physically blocked rotation; paired encoders proved this was not a disconnected motor. A 3.99-in reverse clearance move then enabled a successful retry to 89.951 degrees (0.049-degree error). The robot reversed 6.715 in into Goal 3, but physical contact yawed it to 70.822 degrees, so the pose gate correctly rejected the leg rather than claiming precision. A drive-disabled L1-direction/positive-P3 outtake ran at 35/127, moved 1933 motor-degrees, peaked at 1082 mA, and did not jam. High-resolution camera evidence does not unambiguously show the yellow pin inside the Goal, so end-to-end scoring remains visually unverified. All startup-motion flags were restored false and slot 4 was reinstalled as `Path1Safe`; the MJPEG stream remains live on port 8090. Evidence is in `reports/path1_tuning_2026-08-28/restart_toggle`, `restart_backside`, `restart_clear_turn`, `restart_rear_goal`, and `restart_outtake`.
- `2026-08-28T18:34:17-07:00` - Path 1 orientation contract corrected by the user: the robot starts with its front intake facing the Toggle. The simple Up+A sequence is forward 6 in into the Toggle, reverse 6 in more slowly to the start, then curve to a Goal target 6 in behind and 8 in right of the start in that front-intake frame; the 6-in-behind value belongs to the curve and does not reverse the opening sequence.
- `2026-09-04T16:17:41-07:00` - Reconciled production firmware with the new-robot wiring reported by the operator and the current subsystem code. Drive remains P17/P18 left and P11/P13 right with IMU P12; P1 is now explicitly rear-facing, P6 AI Vision, P7 GPS, P5 claw-arm Rotation feedback, P15 horizontal odometry Rotation, and Toggle active-low ADI-D. Split the formerly aliased P5 arm/odometry objects, moved the conflicting upper-intake declaration back to historical P14 pending physical verification, disabled use of rear P1 as a forward collision gate, and made the Toggle extend on entry to autonomous or driver control and remain extended. D4/UI now label rear P1 and P15 while accepting legacy h5 logs. All 59 pre-existing host tests plus the new P15 schema regression pass, and the ARM firmware builds successfully. No firmware was uploaded and no mechanism or drivetrain was actuated.
- `2026-09-04T19:30:45-07:00` - Applied the operator's final hardware clarification: the upper intake is removed and P14 is empty, so its device declaration, driver binding, route stop calls, and mechanism diagnostic were deleted. Enabled the working P15 horizontal odometer with a zero-inch rotational lever arm because it is mounted at robot center; P5 remains dedicated claw-arm feedback. The existing estimator already fuses valid P7 GPS as a bounded stopped-position correction rather than requiring it continuously. A fresh laptop-side GPS/P15 check could not run because no VEX USB device or `/dev/ttyACM*` was connected; Brain Devices-screen detection proves enumeration on the Brain but not live coordinate quality. All 60 host tests and the ARM build pass, with no startup motion enabled. No firmware was uploaded and nothing moved.
- `2026-09-04T19:34:23-07:00` - Brain USB reconnected and a read-only live P7/P15 sample ran without robot motion. After a brief GPS heading transition from roughly 234 to 239 degrees, a subsequent 10-second/167-frame stationary window was stable: native sensor X/Y medians -24.020/38.504 in, heading median 239.340 degrees clockwise, position spans 0.449/0.252 in, heading span 0.080 degrees, and constant reported RMS error 0.386 in. Applying the configured 6-in-right/6-in-behind mount transform gives robot-center project pose about `(36.39, 15.80, 210.66 deg CCW)`. P15 stayed exactly at -606 centidegrees during the stationary window. The running older image still reports P12 IMU as unavailable (`inf`, status 255); no upload or actuation occurred.
- `2026-09-04T19:37:20-07:00` - Operator supplied surveyed/current robot-center truth `(20,24)`, invalidating the direct P7-to-project pose previously reported near `(34.94,18.06)` by about 16.1 inches. P7 is electrically active and locally stable, but its raw field frame/current origin or the stored axis/mount transform is not aligned to the operator's field coordinates. Do not treat raw P7 as an absolute field pose. Production's start-pose alignment and bounded innovation gates remain necessary; no GPS frame constants were changed from this single stationary correspondence.
- `2026-09-04T19:40:50-07:00` - Armed a read-only P7 timing capture before operator movement and triggered at the first >=2-degree drivetrain-encoder change (`robot_ms=3898`). Over the following 20.040 s, D4 delivered 335 frames with an exact 60-ms interval for all 334 gaps (16.667 Hz). The P7 X/Y/heading tuple changed on every emitted frame, giving 335 unique tuples and an observed new-reading period of 60 ms with no repeats. This establishes that live P7 data is at least as fresh as the firmware's 60-ms telemetry cadence; it cannot establish a faster native camera rate because D4 is the sampling bottleneck. P7 moved from raw `(-0.6186,0.9383,267.53 deg)` to `(-0.6100,1.0096,263.59 deg)` while retaining a reported 0.0098-m RMS error.
- `2026-09-04T19:45:44-07:00` - Captured operator-moved P15 horizontal-odometer displacement from a pre-motion baseline. Raw position changed from -5538 to -2022 centidegrees, a signed +3516-centidegree rotation over a 2.220-s motion/settle interval. Using the configured 2.0-in tracking-wheel diameter, this corresponds to +0.614 in of measured lateral displacement. This validates live P15 response but does not independently validate its wheel diameter, sign, or slip against physical tape distance.
- `2026-09-04T19:52:46-07:00` - Corrected the prior P15 interpretation and ran one user-cleared, webcam-recorded forward test. The first P15 result above used old P5-labeled firmware and is not P15 evidence. After installing corrected `h15` firmware, a guarded one-shot controller targeted 12.0 in using the inherited 2.0-in wheel assumption, slowed near target, and stopped at 70,532 centidegrees / 12.310 assumed inches. However, paired drive encoders reported 17.405 in and raw P7 translation reported 17.223 in, strongly agreeing that the robot physically traveled about 17.3 in rather than 12.0. The P15 counts imply an effective wheel diameter near 2.798 in from P7, consistent with a likely nominal 2.75-in wheel and proving the inherited 2.0-in diameter was wrong for this robot. Do not claim a 12-in physical result. P15 is a forward/longitudinal tracker, not lateral. Restored the one-shot flag false, disabled the incorrect legacy lateral fusion path, passed 60/60 host tests, rebuilt, uploaded no-startup-motion `P15Safe` slot 4 with SHA-256 `f42aed5844755d4d3805b5abc2802091f78d117e076a546ad5906b4e80e625fd`, and left it stopped. Video is `reports/p15_forward_2026-09-04/p15_forward_12.mp4`.
- `2026-09-04T19:58:20-07:00` - Superseded the tentative 2.75/2.80-in P15 wheel interpretation after the operator confirmed two 2-in coupled wheels. The raw 70,532 centidegrees are 1.95922 sensor revolutions, while P7's 17.22283-in translation requires 2.74110 revolutions of a 2-in ground wheel. Their ratio is 0.714758, within 0.000472 of exact 5/7; applying the reciprocal 7/5 transfer gives 17.2343 in, only 0.0115 in from P7. The drive-encoder comparison independently gives a similar 0.70727 ratio. This points to a systematic 5:7 mechanical/coupling ratio rather than random Rotation Sensor error. Added provisional `kForwardOdomSensorToGroundRatio = 7.0 / 5.0`; it is not yet fused and still requires physical coupling inspection plus a repeated tape-ground-truth run.
- `2026-09-04T20:01:45-07:00` - The immediate recorded 6-in repeat disproved a fixed 5:7 P15 transfer and supersedes the prior inference. With the provisional 7/5 conversion active only in the one-shot controller, P15 reported 4.310 in while paired drive encoders reported 3.588 in during motion, then crossed the brake trigger and continued to 8.114 in after the drivetrain stopped at 5.823 in (left/right 5.818/5.828). This direction-changing scale and post-brake spin indicate mechanical coupling slip, intermittent ground contact, or free-spinning coupled wheels; P15 is not presently safe for localization or closed-loop distance. P7 was degraded after the run at 0.187 m / 7.36 in reported error and was excluded. Restored the test flag false, reset the provisional transfer constant to neutral 1.0, kept legacy lateral fusion disabled, passed 60/60 tests, rebuilt, uploaded stopped `P15Safe` slot 4 with SHA-256 `e9a101cebc93eb4b4d9e767542c9b04b48910b439d46703946232cda0a2f0e27`, and retained the 25.53-s video at `reports/p15_forward_2026-09-04/p15_forward_6.mp4`.
- `2026-09-04T20:28:03-07:00` - Ran a no-actuation dual AI Vision experiment after the operator installed a second camera: P6 is robot-forward and P8 is robot-right, aimed at the Goal near `(24,-48)`. Both enumerated as AI Vision type 29 and configured for Circle 21H7 tags. In the saved 12-second run P6 saw no tag; P8 produced 3/120 valid samples, all ID 2 as expected for that Goal, with median horizontal range 11.28 in, bearing +32.59 deg, and elevation -14.01 deg. The few valid readings were internally stable, but the tag was near pixel x=295.5/320 with strongly unequal 11.2/16.0-pixel edge spans, and 2.5% availability is too weak for fusion. P8 therefore remains diagnostic-only pending a measured P8-to-robot extrinsic, more centered view, tape range, and reliable heading. Restored the startup diagnostic flag false; no mechanism or drivetrain moved. All 60 host tests passed, the ARM image built, and stopped slot-4 `DualAISafe` was installed with SHA-256 `52119f055b85b831855ed10e2527c71820823066d9fab995e68439cc55a8c790`.
- `2026-09-04T20:33:25-07:00` - Added P7 reads to the no-actuation dual-camera loop and captured 120 synchronized stationary GPS samples. Direct mount-corrected P7 medians were `(49.13,5.98)` in and `247.93 deg`, with nominal 0.40-in reported error, but stationary Y wandered across 7.17 in. P8 returned no tag during this capture. Comparing the immediately preceding 11.28-in ID-2 P8 observation against the nearest possible face of the Goal centered at `(23.55,-47.09)` gives at least a 45.1-in disagreement with P7, before the unknown P8 translation. The GPS fix is therefore rejected pending field-frame/mount recalibration; it was not fused and nothing moved. Evidence is in `reports/dual_ai_vision_2026-09-04/gps_compare_live_02/`. Restored the no-startup-test source, passed all 60 host tests, rebuilt/uploaded stopped slot-4 `DualAISafe`, SHA-256 `e64f8ba8f92c7df28eb4f00c62b8264f218089d915bdd1f5d96051a6decb2521`.
- `2026-09-04T21:12:18-07:00` - Calibrated the P5 absolute arm targets with P4 forced to zero. The operator-defined start/default pose was stable at 62.49 deg; the Right-arrow pose was stable across three Brain-screen reads at 21.18 deg. Updated the normal program and its regression assertions from the obsolete 324.66/278.17-degree values. The controller remains PD-only (`kP=1.35`, `kD=0.10`, no feedforward): Right selects 21.18 deg and Left selects 62.49 deg, while autonomous holds 62.49 deg. Disabled the calibration-only logger, passed all 60 host tests, built, uploaded slot-1 `ArmTargetsTest`, and started it. Binary SHA-256 is `bf3c21962f592f038b13a338729067d7bed76be26e3c82511614f32847b396d1`.
- `2026-09-04T21:19:26-07:00` - Fixed the two blockers affecting the selectable competition autonomous routines. Live Brain inventory had shown the chassis Inertial Sensor on P10 while `Drive chassis(...)` still addressed empty P12, explaining the prior `imu=inf`, status-255 startup and navigation initialization refusal; firmware and current hardware documentation now use P10. The route lock is now explicitly released in both `disabled()` and `competition_initialize()`, so a completed field autonomous cannot leave L2 selection permanently locked. Three standalone L2 releases still select the next route, with the inter-tap window increased from 1.2 s to 2.5 s for reliable controller entry; L2+B remains the launch chord and is excluded from tap counting. All 60 host tests pass and the ARM firmware builds. No startup-motion flag is enabled and no motion was run. Upload/live P10 verification is pending because the V5 Brain disappeared from USB (`/dev/ttyACM*` absent; no VEX USB device in `lsusb`).
- `2026-09-04T21:23:30-07:00` - Superseded the initial P10 interpretation using the PROS device-type enum: P10 type 8 is the controller radio, while P14 type 6 is the actual Inertial Sensor. Corrected `Drive chassis(...)`, diagnostics, UI labels, tests, and current hardware documentation to IMU P14. Rebuilt and uploaded slot-1 `AutonSelectorFix`, then started it without launching autonomous. A stationary 10-second readback produced 167/167 finite P14 IMU samples (median -0.01 deg, span 0.01 deg, status healthy), zero drivetrain-encoder motion, and finite accelerometer/gyro data. The Brain screen shows the live selector on `2 Cup Auto Red`. The disabled/competition selector-unlock fix and 2.5-second L2 triple-tap window remain active. All 60 host tests pass; installed binary SHA-256 is `d8d13acc6238a97ba8c4e94278119b82736d4a11c682e7b0950407426b6d173a`.
- `2026-09-04T21:31:33-07:00` - Corrected the competition-size behavior for active-low Toggle piston ADI-D. `initialize()` explicitly commands output high/retracted on every program start. Removed both unconditional low/extended writes from `opcontrol()`, so entering driver control before autonomous cannot deploy the mechanism. Competition `autonomous()` deploys immediately, and the L2+B test launch now performs the equivalent synchronous deployment; neither autonomous path retracts afterward, so the piston stays extended through the subsequent opcontrol period. Added regression coverage and state logs; all 60 host tests pass and the ARM image builds, SHA-256 `d6a5ccb1337ec09d2476afc7f389d9fe950bac89909cf3b2a040351b9a5154a8`. Upload is pending because the V5 disconnected from USB immediately before transfer and `/dev/ttyACM*` disappeared.
- `2026-09-04T21:33:02-07:00` - Uploaded the corrected competition-size image through the USB-connected V5 Controller and its paired radio link. Slot 1 is now named `CompetitionAuton`; its INI and binary timestamps are 21:32:16/21:32:17, and the Brain accepted explicit stop/run commands afterward. Controller-link system status identifies Brain `0x4b627a00`. This connection exposes management/download traffic but not the Brain's user serial telemetry, so the new retracted startup output cannot be read back electronically through this single controller channel; it is directly commanded by `initialize()` before opcontrol. No autonomous routine or drivetrain motion was launched.
- `2026-09-04T21:39:48-07:00` - Applied the operator-requested quick Path 2 tune without running motion. The first post-Toggle -75-degree turn is unchanged, but its following preload-Goal reverse target is now a named 12.0 in instead of the prior 11.0 in, adding exactly one inch of travel toward the Goal. The arm's positive-angle/downward return toward the calibrated 62.49-degree default now uses P=1.65, maximum power 85, and minimum power 20; the opposite/upward response remains P=1.35, maximum 70, minimum 16, with shared D=0.10 and unchanged endpoint. All 60 host tests pass, the ARM build succeeds, and `git diff --check` is clean. Binary SHA-256 is `6f534442535aca386be1eef4102aa9b2fb428fea35b05941aba547f021898362`. Upload is pending because the USB Controller disappeared (`/dev/ttyACM*` absent) before transfer and did not re-enumerate during a 10-second retry.
- `2026-09-04T21:42:16-07:00` - Uploaded the tuned `CompetitionAuton` image through the USB Controller/radio link to slot 1 and explicitly started it. Brain filesystem verification shows fresh slot-1 INI/binary timestamps 21:41:58/21:41:59 and binary size 33,439 bytes. No autonomous routine or drivetrain motion was launched; the new program-start state commands Toggle ADI-D retracted until autonomous begins.
- `2026-09-04T21:47:31-07:00` - Changed controller autonomous selection from three standalone L2 taps to exactly two. Hardened chord arbitration so every B rising edge clears the partial L2 tap count/timestamp, and any overlap of L2+B marks that press as a launch chord rather than a selection tap. Updated controller/LCD instructions to `L2x2=SEL L2+B=GO`, passed all 60 host tests, rebuilt, uploaded through the Controller radio link, and explicitly started slot-1 `CompetitionAuton`. Fresh slot files are timestamped 21:47:09; installed binary SHA-256 is `ec01b623a7470829664eb4ed0b6103bd0877614e4579bc198c1c16833d37a75c`. No autonomous motion was launched.
- `2026-09-04T21:51:18-07:00` - Corrected the still-overcounting L2 selector after the operator observed one physical press switching routes. Verified all selector call sites, restored the requirement to three accepted standalone taps, and added actual input qualification: 60-ms stable-state debounce, minimum 80-ms press, and minimum 150-ms separation between accepted releases. A B rising edge or any L2+B overlap clears the entire partial tap sequence. The Controller now displays `AUTON SELECT 1/3` and `2/3`; only 3/3 calls the route selector. All 60 host tests pass, `git diff --check` is clean, and strings in the built binary independently confirm `L2x3=SEL`, `AUTON SELECT %d/3`, and `l2_tap=%d/3`. Uploaded slot-1 `CompetitionAuton` through the Controller; Brain filesystem timestamps are 21:50:43/21:50:44 and binary size 33,551 bytes. The first immediate stop/run management commands encountered a transient radio response-header timeout after the successful upload, so `run 1` was retried alone and Brain status then responded normally. Installed binary SHA-256 is `ccf9e11698331fb50761b871ba8af8506d70fb6bf58d883a069f08c1d8fdbd4c`. No autonomous motion was launched.
- `2026-09-04T21:59:06-07:00` - Applied four operator-requested Path 2 refinements: increased the preload/first-pin deposit dwell from 200 to 350 ms; shifted Stack A from `(24.4,25.5)` to `(23.9,25.0)`, 0.5 in toward field center on both axes; reduced Stack A post-capture pull-in from 2.0 to 1.0 in; and increased Stack B's reverse scoring drive from 16.0 to 24.0 in. Expanded that longer Stack B drive's timeout from 2.6 to 3.6 s so it retains a realistic completion budget. All 60 host tests pass and the ARM build succeeds. Uploaded and started slot-1 `CompetitionAuton` through the Controller; fresh slot timestamps are 21:58:50 and installed binary SHA-256 is `2a42fc992a9793629ca32f27e362094767033df813b2daa731927858a9627158`. No autonomous motion was launched.
- `2026-09-04T22:05:42-07:00` - Applied the operator's final Stack A target `(23.5,25.5)`, moving farther toward field center while restoring the desired higher Y alignment. Slightly accelerated only the arm's downward/default return: P increased 1.65 to 1.80 and maximum power 85 to 90; D=0.10, minimum power 20, calibrated 62.49-degree endpoint, and upward tuning remain unchanged. All 60 tests pass, `git diff --check` is clean, and the ARM image builds with SHA-256 `681d55b5696928a6b14d76f95a5d7e1ec49d9a090f38faa5276645ad8a5c7213`. Upload is pending because the Controller disconnected before transfer and remained absent during a 10-second retry.
- `2026-09-04T22:14:20-07:00` - Added a separately selectable blue 1-pin autonomous while leaving the two-cup route red-only pending operator walkthrough. The blue route shares the same implementation, signed distances, powers, mechanism timing, and phase ordering as red, but applies an exact 180-degree center mirror: start `(-54,13.6076952,180 deg)`, first Goal `(-48,24)`, first Goal heading `240 deg` versus red `60 deg`, post-Goal heading `180 deg` versus red `0 deg`, and final stack `(-24,0)`. Expanded the selector from two to three entries: `1 Pin Auto Red`, `2 Cup Auto Red`, and `1 Pin Auto Blue`; the debounced L2x3 gesture advances between entries and L2+B launches the displayed one. This build also includes the pending Stack A `(23.5,25.5)` and arm-down P=1.80/max=90 tune. All 60 tests pass, ARM build and `git diff --check` succeed, and binary strings confirm the blue entry. Uploaded/started slot-1 `CompetitionAuton` through the Controller; fresh slot timestamps are 22:14:00 and binary size 33,727 bytes. Installed SHA-256 is `b49b2a745d2f446d99c3a5e80f3d576a2237e55739a0b86e7ab7ae086279a87c`. No autonomous motion was launched.
- `2026-09-04T22:32:42-07:00` - Replaced the selector with a controller-only three-press state machine: exactly three completed L2 presses must occur within one 2000-ms window measured from the first accepted release, and each subsequent press requires 300 ms of continuous L2 release to rearm. L2 is still debounced for 60 ms and must be held 80 ms; any B edge or L2+B overlap clears the sequence. Removed all Brain-button/touchscreen selector call sites; static regression now asserts the only `select_auton(+1)` call is behind `l2_tap_count >= 3`. Added `2 Cup Auto Blue` as a fourth entry. Its requested X/Y mirror uses start `(0,63)` at 90 deg, Goal `(24,48)`, Stack A `(24,24)`, Stack B `(48,48)`, relative first turn +75 deg, first-stack score heading 270 deg, Stack-B pickup 225 deg, Stack-B score heading 0 deg, and final heading 270 deg; distances, powers, and mechanism timing remain shared with red. All 60 tests pass, the ARM build succeeds, binary strings confirm all four names and the 3-press UI, and SHA-256 is `2e50cd749f4fcf6b7aa23edacfcf31d566f884b1424a755e1fbbccbb20883034`. Two controller-radio upload attempts failed before transfer because the paired Brain did not answer the response handshake; the prior slot-1 image remains installed.
- `2026-09-04T22:38:35-07:00` - Controller-to-Brain radio communication recovered. Uploaded and started the pending four-autonomous `CompetitionAuton` image in slot 1. Brain filesystem verification shows fresh INI/binary timestamps 22:38:16 and binary size 33,889 bytes. Installed SHA-256 matches the verified build: `2e50cd749f4fcf6b7aa23edacfcf31d566f884b1424a755e1fbbccbb20883034`. No autonomous routine or drivetrain motion was launched.
- `2026-09-04T22:46:48-07:00` - Simplified the controller autonomous selector to raw completed L2 taps: tap 1 opens a two-second window, tap 3 advances exactly once to the next entry in the ordered four-route list, and B/L2+B clears partial selection. Removed the debounce, minimum-hold, and release-rearm filters that could prevent ordinary taps from registering. Applied red two-cup-only tuning: Stack A `(23.5,25.5)` to `(23.8,25.9)`, preload/first-pin Goal fallback 13.5 to 15.0 in, and Stack B capture lead 5.2 to 4.45 in so clamping occurs 0.75 in later; blue retains the prior values. All 60 host tests pass, the ARM build succeeds, and `git diff --check` is clean. Uploaded and started slot-1 `CompetitionAuton`; Brain filesystem timestamps are 22:46:23/22:46:24 and the installed build SHA-256 is `56aa8289362a4c6a3da724ebef99ad1a9e1a45e86ec4f291dd8125814c3f88fd`. No autonomous routine or drivetrain motion was launched.
- `2026-09-04T22:55:26-07:00` - Corrected the latest two-cup route symmetry/tuning. Stack A moved 0.3 in toward the origin on both axes, from red `(23.8,25.9)` to `(23.5,25.6)`, and blue is now its exact origin-opposite `(-23.5,-25.6)`. Increased the post-preload-Goal clearance another 1.5 in, 15.0 to 16.5 in, and applied the same 16.5-in magnitude to blue. Added compile-time proof of the selector cycle `1R -> 2R -> 1B -> 2B -> 1R`; host regression additionally proves `autonomous()` invokes the stored-selection dispatcher and each enum maps once to its intended routine. All 60 tests pass, the ARM build succeeds, and `git diff --check` is clean. Changed the V5 Brain system team number from `000000` to `1862X` and independently read it back as `1862X`. Uploaded and started slot-1 `CompetitionAuton`; fresh slot timestamps are 22:55:07 and installed SHA-256 is `eb3fd709bc8a61a2b44a8c64d6839dbb015390dd5ae2db5cabaf5e301f3f70d7`. No autonomous routine or drivetrain motion was launched.
- `2026-09-04T23:03:35-07:00` - Applied the final requested first-stack waypoint: red Stack A is `(24.5,26.1)` and blue Stack A is its exact origin mirror `(-24.5,-26.1)`. Updated regression expectations; all 60 host tests pass, `git diff --check` is clean, and the ARM firmware builds successfully. Prepared the accumulated production source, tests, tools, documentation, and untracked analysis source scripts for the requested GitHub `main` update while excluding generated recordings/logs and other large diagnostic artifacts.
- `2026-09-04T23:13:40-07:00` - Updated the two-cup first-stack waypoint to red `(23.67,26.5)` and exact origin-mirrored blue `(-23.67,-26.5)`. Reduced the forward retreat immediately after scoring the first stack by 0.5 in, from 9.75 to 9.25 in. All 60 host tests pass, `git diff --check` is clean, and the ARM firmware builds successfully with SHA-256 `12306e50da88d6fccb3c28a4f132aaddf29ac621510d10e3e1f190d26cce237b`. This build has not yet been flashed or pushed.
- `2026-09-05T00:11:29-07:00` - Added a diagnostics-only autonomous observer for both AI Vision sensors: P6 forward-facing and P8 robot-right-facing. It reads cameras only after all four drivetrain motors and P14 gyro indicate at least 300 ms stationary, computes provisional tag-derived positions against mapped Goal faces, and reports `vision_gps_delta`, `vision_fused_delta`, `gps_fused_delta`, and a display-only similarity percentage. It never commands actuators, never invokes an estimator update, emits `correction_applied=0`, and AI Vision fusion is explicitly disabled. Both camera translations remain assumed zero pending measurement. Also changed red two-cup Stack A from `(23.67,26.5)` to `(23.0,27.0)` and its exact blue mirror to `(-23.0,-27.0)`. All 61 host tests pass, `git diff --check` is clean, and the ARM image builds with SHA-256 `c657da856c0160de54aad6441e2ef94c90b8add9e9166ae04bd2e4685b8aea4e`. Not yet flashed or pushed.
- `2026-09-05T01:03:36-07:00` - Finalized Stack A at red `(23.0,26.5)` and exact blue mirror `(-23.0,-26.5)`. The operator supplied the currently held P5 claw-arm angle as 63.2 deg; updated the default/start/autonomous hold target from 62.49 to 63.2 deg and restored the temporary no-actuation calibration flag to false. All 61 host tests pass, `git diff --check` is clean, and the production ARM build SHA-256 is `5e8afa720873404df85fa95e0e208ec10fdcb6d7ecdd1599ede99d4335ed743e`. Uploaded and started slot-1 `CompetitionAuton` through the Controller radio; fresh slot timestamps are 01:03:09, binary size is 35,744 bytes, and Brain team number reads back as `1862X`. No autonomous routine or drivetrain motion was launched.
